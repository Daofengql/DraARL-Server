# 04. 群组与互联

## 1. 群组查询与基础操作（JWT + Approved）

### `GET /api/groups`

参数：`page`、`page_size`、`keyword`

返回示例：

```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "items": [
      {
        "id": 1001,
        "name": "深圳应急群",
        "type": 1,
        "is_joined": true,
        "is_owner": false,
        "online_count": 12,
        "total_count": 53
      }
    ],
    "total": 1,
    "page": 1,
    "page_size": 20
  }
}
```

### `GET /api/group/list`（兼容）

### `GET /api/groups/:id`

### `GET /api/groups/:id/devices`

返回设备列表含 `disable_send/disable_recv`（仅设备级状态）。

### `POST /api/groups`

### `POST /api/group/create`（兼容）

群组自身的旧 `callsign` 字段和设备准入白名单已经停用；请求中的旧字段会被忽略，响应也不再返回。

请求：

```json
{
  "name": "深圳私有群",
  "type": 2,
  "password": "123456",
  "note": "内部使用"
}
```

### `POST /api/groups/search`

请求：

```json
{ "keyword": "深圳", "page": 1, "page_size": 20 }
```

### `POST /api/groups/:id/join`

请求：

```json
{ "password": "123456" }
```

### `GET /api/groups/:id/members`

说明：管理员或群主可查看。

### `POST /api/groups/:id/leave`

说明：仅私有群支持退出，群主不能退出自己创建的群。

## 2. 群组管理（Admin/GroupOwner）

说明：群组不提供设备准入名单或永久封禁；群主可以临时调整当前组内普通设备的设备级收发状态。

### `PUT /api/groups/:id`

请求：

```json
{
  "name": "深圳私有群-新",
  "type": 2,
  "password": "654321",
  "status": 1,
  "note": "更新备注"
}
```

### `DELETE /api/groups/:id`

### `DELETE /api/groups/:id/devices/:deviceId`

功能：踢出设备并移入默认群组。

### `PUT /api/groups/:id/devices/:deviceId/comm-control`

权限：管理员或当前群主。设备必须仍然属于路径指定的群组。

```json
{
  "disable_send": true,
  "disable_recv": false,
  "reason": "疑似持续发射，等待排查"
}
```

两个状态均为设备级全局状态，设备切换群组后仍然生效。接口只修改明确提交的字段；
设备所有者仍可在自己的设备管理接口中恢复状态。`reason` 可选，最多 500 个字符，写入审计日志。

设备所有者或管理员也可使用设备接口：

### `PUT /api/devices/:id`

请求体可包含：

```json
{ "disable_send": true, "disable_recv": false }
```

## 3. 兼容旧接口说明

- `POST /api/group/update`
- `POST /api/group/delete`

旧更新路径从 JSON 请求体读取 `id`，旧删除路径从 JSON 请求体或查询参数读取 `id`。
两者继续执行“管理员或群主”对象级权限校验；新客户端仍建议使用
`PUT/DELETE /api/groups/:id`。

## 4. 虚拟互联组（Admin）

### `POST /api/group-links`

请求：

```json
{ "name": "华南互联组", "note": "跨群互联", "status": 1 }
```

### `GET /api/group-links`

### `GET /api/group-links/:id`

### `PUT /api/group-links/:id`

### `DELETE /api/group-links/:id`

### `GET /api/group-links/available-targets`

功能：获取可被关联的目标群组（非虚拟组）。

### `GET /api/group-links/:id/targets`

### `POST /api/group-links/:id/targets`

请求：

```json
{ "target_group_id": 1001 }
```

返回：

```json
{ "code": 200, "message": "添加成功", "data": { "link_group_id": 2001, "target_group_id": 1001, "target_count": 3 } }
```

### `DELETE /api/group-links/:id/targets/:targetId`
