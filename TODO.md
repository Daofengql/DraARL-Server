# 幽灵设备多端在线、多频道接收与消息历史改造 TODO

> 本文替代旧 TODO，作为本次实时通信架构改造的实施清单。
>
> 核心目标：特殊幽灵设备允许多端在线，每个在线会话可以同时接收多个频道，但只能向一个选定频道发送；标准 UDP 实体设备继续保持单端在线。

## 用户原始设想（需求说明）

> 当前系统基本只能“单听、单发”，并且用户在通信列表里似乎只能看到自己设备的消息记录，Android 客户端也存在同样的问题。希望取消特殊幽灵设备的单端限制，允许同一账号多端在线；幽灵设备支持“多收单发”，可以同时接收多个频道/群组的消息，但发送时只使用一个选定频道。
>
> 多端能力只属于特殊幽灵设备，用于同一账号存在多台移动设备、浏览器或其他客户端的场景。标准 UDP 实体设备仍然只能单端在线，继续维持现有冲突与唯一在线约束。
>
> 消息历史应拆出专用 API，不再直接把通信记录接口当作聊天记录接口。消息与通信记录可以继续来源于同一张表，但采用不同的查询条件和权限边界：通信记录按本人设备或审计用途查询，消息历史按有权访问的频道/群组查询。这样其他客户端可以看到群组内完整历史，而不是只能看到自己设备的记录，也便于后续完善消息日志。

以上设想整理为以下产品形态和实施边界：

- 特殊幽灵设备取消单端限制，允许同一账号在多台移动设备、多个浏览器或其他客户端上同时在线。
- UDP 标准实体设备仍然只能单端在线。实体设备继续保持现有唯一身份和冲突保护，不能因幽灵设备支持多端而被一并放宽。
- 幽灵设备支持“多收单发”：可以同时订阅并接收多个频道/群组的消息，但发送时只能选择一个明确的目标频道。
- 同账号的多个幽灵客户端会话彼此独立；一台移动设备上线、切换频道或下线，不应覆盖或踢掉其他移动设备。
- 增加专用的频道消息历史 API。它与通信记录 API 可以来源于同一张 `comm_records` 表，但使用不同的查询条件、权限语义和响应模型。
- 通信记录继续服务于“本人设备发信记录、通信审计和管理员日志”；频道消息 API 则按群组查询该频道中有权查看的完整消息历史。
- Web、Android 及其他客户端查看消息时，应按群组读取其他发送者的历史消息，而不是只能读取当前账号设备的记录。
- 新的消息查询边界也应让消息日志、分页同步、语音回放、发送者展示和后续检索功能更容易维护。

这不是简单删除幽灵设备冲突判断。实现时必须把“账号/平台身份”“客户端安装实例”“临时在线会话”“唯一发送频道”和“多个接收订阅”拆开，同时保持实体设备单端规则不变。

## 当前进度（2026-08-05）

| 阶段 | 状态 | 已有证据 / 剩余工作 |
|---|---|---|
| P0 安全基线 | 服务端完成 | 通信记录详情授权、统一群组 ACL、幽灵录音会话键及 MySQL 回归测试已提交 |
| P1 频道消息 API | 服务端完成 | 独立列表/详情、游标、发送者快照、互联群组有界查询和百万行 MySQL 验收已完成 |
| P2 客户端消息迁移 | 完成 | Web 与 Android 均已切换频道消息 API；Android 游标、结构化发送者和来源频道映射已通过自动化门禁 |
| P3 Session 基础 | 服务端完成 | GhostSession、安装实例偏好、订阅关系和原子 routing API 已完成 |
| P4 Web 多端多收 | 完成 | WebSocket 多 Session、来源频道、收听选择、分流解码和可控混音已完成 |
| P5 UDP 多端多收 | 完成 | 服务端协议、鉴权、Session 路由和 fan-out 已完成；Android 已接入现代认证、Session tag、多收路由和来源流隔离 |
| P6 节点互联 | 服务端完成 | 功能位 8、跨边缘多 Session、迁移、重确认和去重已完成 |
| P7 验收 | 自动化完成，真机待验 | 服务端全量/专项门禁与 Android `test lintDebug assembleDebug` 已通过；双 Android 真机和网络切换 E2E 待完成 |
| P8 灰度与清理 | 未开始 | Android 完成并通过完整验收后执行；legacy 兼容路径当前保留 |

进度只按已有代码和测试证据勾选。Android 自动化完成不代表真机 E2E 已完成；涉及双机、网络切换和实际听感的条目在取得设备验收证据前保持未勾选。

Android 适配已在 `DraARL-Android` 的 `5c9f645`、`e950e4b`、`ab2feb6`、`4b1661b` 等本地提交中分阶段完成，最终验证提交为 `d9a8fbd`。Ubuntu VM 隔离副本使用 JDK 21、Android SDK 36.1、NDK `28.2.13676358` 和 CMake `3.22.1` 执行 `test lintDebug assembleDebug`，53 个 Gradle task 成功，生成 44 份测试 XML、lint HTML 报告和约 49 MB 的 debug APK。该证据覆盖编译、单元测试、静态检查和打包，不替代两台 Android 真机同时在线及网络切换测试。

## 0. 目标、边界与术语

### 0.1 必须保持的不变量

- [x] 仅对幽灵设备开放多端在线：UDP `DevModel/SSID 101-104` 与 WebSocket `105`；`100` 继续预留。
- [x] UDP 幽灵设备 `101-104` 和 WebSocket 幽灵设备 `105` 均纳入新会话模型。
- [x] 标准 UDP 实体设备继续使用 `owner_id + ssid` 作为唯一在线身份。
- [x] 标准 UDP 实体设备的在线冲突检查、旧连接保护和节点迁移规则不得放宽。
- [x] 一个幽灵设备会话只能拥有一个发送频道 `tx_group_id`。
- [x] 一个幽灵设备会话可以订阅多个接收频道 `rx_group_ids`。
- [x] `tx_group_id` 必须同时存在于 `rx_group_ids`，确保发送端可以监听所选频道。
- [ ] 发送权限与接收权限分别校验，不能因为可以查看群组就自动获得发言权。
- [x] 私有群组必须持续满足群主、管理员或已验证成员条件。
- [x] 禁用、删除、转为虚拟群组或撤销成员资格后，相关会话必须及时停止收发。
- [x] 幽灵设备多端不得改变实体设备协议行为、数据库唯一约束和运行时索引语义。

### 0.2 术语

- [x] `client_instance_id`：客户端安装实例的稳定随机 UUID；用于区分同一账号下的多台手机、多个桌面客户端或浏览器配置实例，不使用硬件指纹。
- [x] `session_id`：每次在线连接由服务端签发的临时不可预测标识；断线重连可以生成新值。
- [x] `session_tag`：UDP 数据包热路径使用的短会话标识；只用于快速定位已认证会话，不能替代 JWT 认证和所有权校验。
- [x] `tx_group_id`：当前唯一发送频道。
- [x] `rx_group_ids`：当前直接订阅的接收频道集合。
- [x] `source_group_id`：消息实际发出时所在的源频道。
- [x] `requested_group_id`：客户端正在查看的频道；互联转发时可能与 `source_group_id` 不同。

### 0.3 产品规则需要在编码前锁定

- [x] 将当前固定的每账号 `8` 个幽灵 Session 上限改为可配置，并补配置边界测试。
- [x] 将当前固定的每 Session `16` 个接收频道上限改为可配置，并补配置边界测试。
- [x] “单发”采用每会话只有一个 `tx_group_id` 的语义。
- [ ] 决定是否增加账号级语音话权锁；建议同一账号同一时刻只允许一个幽灵会话发起 PTT，避免两个移动设备同时发言。
- [ ] 明确文本消息是否受账号级话权锁限制；建议文本不占用语音 PTT 锁，但仍只能发往当前 `tx_group_id`。
- [x] 订阅实体群组后自动接收其当前互联通信域中的消息，并按 Session 去重。
- [x] 多个无关频道同时出现语音时：Web 采用按来源流隔离、按频道开关和音量的可控混音；Android 按完整讲话流隔离解码并按到达顺序串行排队播放。
- [x] 第一版消息历史使用当前启用的互联拓扑，并已在 API 文档中说明。

## 1. 现有问题修复与安全基线

> 这一阶段必须先完成，不能等待多端功能上线。

### 1.1 通信记录越权修复

- [x] 为 `GET /api/comm-records/:id` 增加记录级授权，禁止任意登录用户按递增 ID 获取其他用户录音。
- [x] 管理员后台模式允许查看任意通信记录。
- [x] 普通用户仅能通过“本人通信记录”语义访问本人设备或本人幽灵设备产生的记录。
- [x] 群组消息详情不得复用上述本人记录授权，改由新的消息 API 按频道权限授权。
- [x] 只有通过记录授权后才能生成或返回 MinIO/S3 音频签名 URL。
- [x] 添加普通用户、群组成员、被移除成员、管理员和不存在记录的授权测试。

### 1.2 统一群组访问规则

- [x] 删除 `comm_records.go` 中基于“拥有群组设备或曾经发过消息”的 `userHasGroupAccess` 授权语义。
- [x] 消息历史、实时订阅、在线设备列表和切换发送频道统一复用 `canViewGroup`/`requireGroupViewAccess`。
- [x] 群组必须存在、启用、非虚拟且类型受支持。
- [x] 公开群组允许已审核用户访问。
- [x] 私有群组只允许管理员、群主或已验证成员访问。
- [x] 无权限时返回明确的 `403`，不得静默降级为“只返回本人记录”。
- [x] 无效 `group_id` 返回 `400`，不存在的群组返回 `404`。
- [x] 添加成员退出或被移除后无法继续读取历史、订阅实时消息的回归测试。

### 1.3 现有录音会话键修复

- [x] 将录音缓冲的“运行时会话键”和最终持久化的 `device_id` 分离。
- [x] 标准实体设备继续使用稳定的实体设备 ID 作为录音来源。
- [x] Web 幽灵设备不得继续只使用 `-userID` 作为录音缓冲键。
- [x] UDP 幽灵设备不得继续使用默认 `device_id=0` 作为所有幽灵设备共享的录音缓冲键。
- [x] 幽灵设备录音缓冲使用唯一 `session_id` 或等价的内部会话键。
- [x] 持久化到 `comm_records` 时，幽灵设备仍保存 `device_id=0`、实际 `user_id` 和平台 `device_ssid`。
- [x] 添加两个幽灵设备在不同通信域同时发言时不会串音、合并或互相覆盖的测试。

## 2. 拆分频道消息 API

### 2.1 API 语义

- [x] 保留 `/api/comm-records` 作为“本人发信记录/设备审计记录”接口。
- [x] 保留管理员通过 `/api/comm-records?admin_mode=true` 查看全局通信记录的能力。
- [x] 新增 `GET /api/groups/:group_id/messages`，专门获取频道消息历史。
- [x] 新增 `GET /api/groups/:group_id/messages/:message_id`，专门获取单条频道消息及音频地址。
- [x] 消息 API 不接受 `admin_mode`，管理员权限通过统一群组 ACL 判断。
- [x] 消息 API 返回频道内所有可见发送者的消息，不按当前用户设备过滤。
- [x] 消息 API 仅返回已完成记录 `status=2`；上传中、失败和不完整记录不进入普通消息流。
- [x] 支持 `message_type=voice|text|all` 筛选。
- [x] 支持稳定游标分页，不再以 offset/page 作为实时消息流的首选分页方式。
- [x] 游标包含 `start_time + id`，保证相同时间戳下顺序稳定。
- [x] 响应返回 `next_cursor`、`has_more` 和服务端时间。
- [x] 为旧 Web/Android 迁移期保留现有 `/api/comm-records?group_id=...`，待客户端全部升级后再废弃群组消息语义。

### 2.2 消息响应模型

- [x] 定义独立的 `MessageResponse`，不要直接暴露 `CommRecordResponse`。
- [x] 返回 `id`、`message_type`、`source_group_id`、`requested_group_id`。
- [x] 返回发送时间、结束时间、语音时长和消息状态。
- [x] 返回结构化发送者信息：`user_id`、`username`、`callsign`、`nickname`、`ssid`、`dev_model`、`is_ghost`。
- [x] 文本消息返回 `text_content`。
- [x] 语音消息返回授权后的 `audio_url`、`audio_size` 和音频格式信息。
- [x] 客户端不再需要通过 `device_name` 字符串拆分呼号和 SSID。
- [x] 消息时间统一返回 RFC 3339 Nano UTC；旧通信记录字段在兼容期保持不变。

### 2.3 同表数据模型整理

- [x] 第一阶段继续以 `comm_records` 作为语音与文本消息的共同来源，不新建重复消息主表。
- [x] 增加明确的 `message_type` 字段，停止长期依赖 `audio_path` 的 `text:` 前缀判断。
- [x] 增加独立 `text_content` 字段，允许正常的 UTF-8 文本长度，不再受 `varchar(255)` 音频路径限制。
- [x] 保留旧 `text:` 数据的兼容读取和一次性迁移脚本。
- [x] 增加发送者快照字段，包含发送时的呼号、用户名、昵称和设备型号。
- [x] 设备删除、转让或用户修改呼号后，历史消息优先使用发送快照，不被错误改名或误判成幽灵设备。
- [x] 保留 `group_id` 作为 `source_group_id` 的现有存储语义，并在代码层统一命名。
- [x] 增加索引 `(group_id, status, start_time, id)`，并已使用百万行 MySQL `EXPLAIN ANALYZE` 验证。
- [ ] 为文本类型筛选增加合适索引，避免使用 `audio_path LIKE 'text:%'` 扫描大表。

### 2.4 互联群组历史

- [x] 第一版复用当前启用的互联关系计算频道可见消息集合。
- [x] 请求频道本身必须先通过 ACL；不能因为查询互联关系绕过私有群组授权。
- [x] 私有群组与公开群组互联后，实时投递和历史可见范围使用同一当前拓扑语义。
- [x] 同一条消息通过多个互联路径可达时只返回一次。
- [x] 响应中保留真实 `source_group_id`，客户端可显示消息来自哪个频道。
- [ ] 如果需要严格还原“发送当时实际投递范围”，设计不可变的路由域/投递范围快照；不能用当前互联拓扑伪造历史事实。

### 2.5 查询实现与测试

- [x] 将通信记录查询和频道消息查询放入独立 repository/service，避免在 Gin handler 中继续堆叠 SQL 与权限分支。
- [x] 统一 JOIN 发送者、设备和群组信息，处理已删除关联记录。
- [x] 添加公开群组、私有群组、互联群组、禁用群组和已删除发送设备测试。
- [x] 添加游标边界、同时间戳、多页并发插入和去重测试。
- [x] 添加消息详情不能越权获取音频 URL 的端到端测试。

## 3. 幽灵客户端实例与在线会话模型

### 3.1 稳定客户端实例

- [ ] Web 与 Android 已生成并持久化随机 UUID `client_instance_id`；iOS、Windows 和 macOS 待适配。
- [ ] Web 与 Android 已按安装范围持久化 `client_instance_id`；其他原生客户端待适配。
- [ ] Web 与 Android 已使用随机 UUID 且不使用 IMEI、Android ID、MAC 或其他硬件指纹；其他原生客户端待确认。
- [ ] Web JWT/WS 与 Android UDP 认证已提交 `client_instance_id` 和能力版本；其他原生客户端待适配。
- [x] 老客户端没有 `client_instance_id` 时进入 legacy 单实例槽位，并继续保持同平台单端限制。
- [x] 新客户端不得仅凭 `client_instance_id` 获得权限；服务端始终先验证 JWT 和账号状态。

### 3.2 临时会话

- [x] 新增统一 `GhostSession` 领域模型。
- [x] 字段包含 `session_id`、`session_tag`、`client_instance_id`、`owner_id`、`username`、`callsign`、`dev_model`、`ssid`。
- [x] 字段包含 `transport`、连接端点、创建时间、最后活动时间和连接状态。
- [x] 字段包含 `tx_group_id`、`rx_group_ids`、`disable_send`、`disable_recv` 和能力集合。
- [x] `session_id` 使用服务端生成的不可预测 UUID，不使用 `userID`、SSID 或可枚举自增 ID。
- [x] `session_tag` 只在当前在线会话集合内唯一，并与账号、平台、地址和会话状态联合验证。
- [x] 一个 `client_instance_id` 可以重连并替换自己的旧会话，但不能替换同账号的其他客户端实例。
- [x] 旧会话延迟清理时只能删除自己，不能误删重连后的新会话。
- [x] 达到会话上限时返回明确错误和当前会话摘要，不随机踢掉其他移动设备。

### 3.3 偏好持久化

- [x] 现有 `user_device_preferences(user_id, dev_model)` 继续作为 legacy 客户端的默认发送频道。
- [x] 为多实例客户端按 `user_id + dev_model + client_instance_id` 保存偏好。
- [x] 持久化每个客户端实例的最后 `tx_group_id` 和 `rx_group_ids`。
- [x] 使用偏好主表加订阅关系表，未把接收集合编码为逗号字符串。
- [x] 客户端首次升级时，以现有平台 `last_group_id` 初始化 `tx_group_id` 和首个接收频道。
- [x] 删除用户时级联删除实例偏好和订阅。
- [x] 删除群组时清理相关发送偏好与接收订阅。
- [x] 服务启动时不把持久化偏好误认为在线会话。

## 4. UDP 幽灵设备多端协议

### 4.1 协议扩展

- [x] 为幽灵 JWT 认证定义版本化 payload，携带 `client_instance_id`、能力位和协议小版本。
- [x] 保持旧版“DATA 仅为 JWT 字符串”的认证兼容路径。
- [x] 认证成功响应返回 `session_id`、UDP `session_tag`、初始 `tx_group_id` 和 `rx_group_ids`。
- [x] 旧客户端仍能读取认证响应首字节状态码；新增字段不会使旧客户端把成功响应误判为错误详情。
- [x] 明确 DraARLv1 头部 `Reserved[4]` 的新语义并更新协议文档。
- [x] 现代客户端上行包使用 `Reserved[4]` 携带 `session_tag`。
- [x] 服务端下行语音/文本包使用 `Reserved[4]` 携带 `source_group_id`。
- [x] 认证、心跳与媒体包的 Reserved 语义已分别定义。
- [x] UDP 路由明确拒绝超出非零 32 位无符号范围的群组 ID，HTTP 消息路由参数同步按 32 位解析。
- [x] `session_tag` 与 Session、账号、平台、端点联合校验，并覆盖伪造 tag/端点测试。
- [x] UDP 地址发生 NAT 重绑定时要求重新认证，不能只凭 username/SSID 或 tag 抢占会话。

### 4.2 UDP 运行时索引

- [x] `UDPGhostManager` 的现代幽灵主索引改为按 `session_id` 管理，旧 `username-ssid` 索引仅保留兼容用途。
- [x] 增加 `session_tag -> session` 热路径索引。
- [x] 增加 `owner_id + dev_model + client_instance_id -> current session` 重连索引。
- [x] 增加 `group_id -> session_id -> session` 接收订阅索引。
- [x] 同一会话订阅多个群组时允许出现在多个接收索引中。
- [x] 更新订阅时在一次锁操作中完成旧索引删除和新索引添加。
- [x] UDP 超时、主动下线、JWT 失效和实例重连均按 `session_id` 精确清理。
- [ ] `Remove(username, ssid)` 等旧 API 仅服务 legacy 路径，逐步替换为 Session API。
- [x] 保留标准实体设备的 `owner_id + ssid` 运行时索引和冲突逻辑，不迁移到 GhostSession 多实例语义。

### 4.3 UDP 包鉴权与路由

- [x] 每个现代幽灵设备上行心跳、语音和文本包定位到唯一已认证 Session。
- [x] 校验包中 username、SSID、DevModel、session tag 和网络端点与 Session 一致。
- [x] 上行消息的目标频道只取服务端 Session 的 `tx_group_id`，不信任媒体包自行声明任意群组。
- [x] 下行 fan-out 根据 `rx_group_ids` 选择 Session，不再只比较单一 `Device.GroupID`。
- [x] 发送源精确排除当前 `session_id`；同账号同平台的其他端仍能收到消息。
- [x] 一个 Session 因多个订阅或互联路径命中时只发送一个 UDP 包。
- [x] 接收禁用、会话过期或群组权限失效时不进入 fan-out 快照。
- [x] 接收索引变化后正确失效 `domainReceiverCache`。
- [x] fan-out 缓存项携带 Session 身份，不再以 `username + ssid` 认定所有同平台客户端都是发送源。

## 5. WebSocket 幽灵设备多端

### 5.1 连接管理器

- [x] 删除现代 WebSocket 握手阶段“同一用户已有幽灵设备在线即 409”的冲突规则，仅 legacy 槽位保留 `409`。
- [x] `WSConnectionManager.ghostDevices` 从 `userID -> device` 改为 `sessionID -> device`。
- [x] 增加 `userID -> set(sessionID)` 辅助索引，用于状态查询、限额和账号下线。
- [x] `getDeviceKey` 和 `GetIdentifier` 纳入 `session_id`，不再只返回 `ghost-userID`。
- [x] 全局群组索引按接收订阅维护，而不是按单一 `GroupID` 维护。
- [x] 广播到多个群组时按 `session_id` 去重。
- [x] 断开连接、心跳超时和写队列失败时只清理对应 Session。
- [x] 删除重复的旧 `GhostDeviceManager` 单用户状态，以统一 Session 注册表和 `WSConnectionManager` 为准。

### 5.2 WebSocket 协议

- [x] 认证成功消息返回 `session_id`、`client_instance_id`、`tx_group_id` 和 `rx_group_ids`。
- [x] 增加 `routing_updated` 事件，覆盖订阅撤销和发送频道变化。
- [x] 下行语音/文本消息携带 `source_group_id`。
- [x] 保持旧二进制 DraARLv1 消息兼容，定义 `multi_receive_v1` 与 `source_group_v1` 能力。
- [x] 旧 Web 客户端没有多频道能力时只订阅一个频道。
- [ ] 当前同一浏览器 profile 的标签页共享 `client_instance_id`，新标签页重连会替换旧标签页；如需多标签页并存，应增加标签页实例层级或改用 `sessionStorage`，且不能破坏安装级偏好。

## 6. 发送频道与接收订阅 API

### 6.1 会话 API

- [x] 新增 `GET /api/radio/sessions`，仅返回当前账号自己的幽灵在线会话。
- [x] 返回平台、客户端实例、连接类型、在线时间、最后活动时间、发送频道和接收频道。
- [x] 新增 `PUT /api/radio/sessions/:session_id/routing`。
- [x] 请求体使用 `tx_group_id` 和去重后的 `rx_group_ids`。
- [x] 更新前一次性验证所有群组存在、启用、非虚拟并且当前用户有权访问。
- [x] 整个更新在逻辑上原子化；任一群组失败时不修改运行时或持久化状态。
- [x] 先提交持久化偏好，再投影到运行时索引；持久化或投影失败会回滚旧偏好/路由。
- [x] 更新成功后返回服务端规范化后的完整路由状态。
- [x] 新增 `DELETE /api/radio/sessions/:session_id`，允许用户主动断开自己的指定会话。
- [x] 用户可以按 ID 断开自己的任一 Session；新连接默认只替换相同安装实例，不踢掉其他端。

### 6.2 旧 API 兼容

- [x] `/api/radio/group` 对 legacy 客户端继续按 `dev_model` 更新单一群组。
- [x] 新客户端调用旧接口时可携带 `session_id`；存在多个同平台 Session 且未指定 Session 时返回 `409 ambiguous_session`。
- [x] `/api/radio/status` 保留旧单会话响应到客户端迁移完成。
- [x] `/api/radio/sessions` 返回完整会话列表，不随意选择一个连接伪装成唯一在线端。
- [x] `/api/radio/conflict` 对新多端客户端不再报告同平台冲突；legacy 客户端维持原行为。
- [x] 错误码文档覆盖会话不存在、订阅上限、频道权限失效、会话歧义、legacy 降级和 WebSocket 策略关闭。

## 7. 半双工与单发约束

- [x] 每个 Session 的语音和文本上行始终路由到唯一 `tx_group_id`。
- [x] 保留现有通信域半双工仲裁，防止同一群组/互联域出现多个讲话者。
- [x] 半双工讲话者 key 纳入 Ghost Session，不把同用户同平台的多个会话误认为同一个讲话源。
- [ ] 如果启用账号级 PTT 锁，以 `owner_id` 为 key 建立带租约和超时的原子话权状态。
- [ ] 账号级 PTT 锁与通信域话权锁的获取顺序固定，避免死锁和部分占用。
- [ ] 任一话权获取失败时释放已获得的另一层租约。
- [ ] 断线、超时、切换发送频道和 PTT 结束时及时释放话权。
- [ ] 同一 Session 在 PTT 期间禁止切换 `tx_group_id`；返回明确冲突状态。
- [ ] 添加同账号双端、不同账号同域、同账号不同域和互联域并发测试。

## 8. 节点互联与边缘接入

> 多端会话必须覆盖中心直连和边缘节点，不能只在单机模式可用。

- [x] 现有实体设备互联身份继续保持 `owner_id + ssid`。
- [x] 幽灵设备互联身份扩展为 `owner_id + ssid + client/session identity`。
- [x] `DeviceGrant`、中心 active identity 和 Session 确认增加可选幽灵实例字段；撤销消息继续使用全局唯一 `session_id + session_epoch` 精确定位，避免重复暴露实例字段。
- [x] legacy 实体设备与 legacy 幽灵设备保持旧身份编码，避免无条件破坏节点协议。
- [x] 中心不得因为第二个同平台幽灵 Session 上线而迁移或撤销第一个 Session。
- [x] Session 路由携带唯一发送频道和接收订阅所需信息。
- [x] Relay 上行继续以服务端 Session 的 `tx_group_id/domain_id` 为准。
- [x] Relay 下行向边缘多订阅 Session 投递时按 Session 去重。
- [x] 节点会话报告、批量确认、重连恢复和撤销按扩展身份精确处理。
- [x] `routesync.PublishIdentity` 等仅按 `user_id + dev_model` 发布的路径改为支持幽灵实例，不影响实体设备。
- [x] 设计节点协议能力协商；旧边缘节点不支持多端时应明确降级或拒绝，不得静默覆盖会话。
- [x] 制定滚动升级顺序：数据库迁移、中心兼容新旧协议、边缘逐个升级、客户端按能力灰度；当前版本没有独立运行时开关。
- [x] 增加中心直连、边缘直连、边缘切换、控制链路断线恢复和跨节点同账号多端测试。

## 9. Web 客户端改造

- [x] 生成并持久化 Web `client_instance_id`。
- [x] 认证后保存当前 Web 连接的 `session_id`。
- [x] 移除“账号已在其他页面建立电台连接”的现代会话单端阻断 UI。
- [x] 增加发送频道选择与接收频道多选控件。
- [x] 明确显示当前唯一发送频道。
- [x] 接收频道达到 16 个时禁用额外选项；routing API 失败和权限失效通过页面错误状态或 `routing_updated` 反馈。
- [x] 实时消息按服务端下行的 `source_group_id` 入库和分栏，不使用当前选中频道猜测来源。
- [x] 服务端按 Session 去重互联和多订阅投递，Web 按来源频道与发送者隔离实时流。
- [x] 历史消息改用 `/api/groups/:group_id/messages`。
- [x] 前端使用结构化 sender，不再通过 `device_name` 拆分 SSID。
- [x] 使用游标加载更早消息，并处理实时消息与历史消息合并。
- [x] 支持切换查看所有接收频道，并按频道开关/音量进行可控混音。
- [ ] 同一浏览器 profile 的多标签页当前共享安装实例并互相替换；需要补独立标签页会话设计与回归测试，同时确保移动端不受影响。

## 10. Android 客户端改造

- [x] 生成并安全持久化 Android `client_instance_id`。
- [x] 扩展 UDP JWT 认证请求与成功响应解析。
- [x] 在后续上行心跳、文本和语音包中携带 `session_tag`。
- [x] 解析下行媒体包的 `source_group_id`，不得继续使用当前 `selectedGroupId` 代替来源频道。
- [x] 本地消息缓存继续按 `account_key + group_id` 隔离，并以真实来源频道保存实时消息。
- [x] 增加唯一发送频道和多接收频道设置，并将消息查看频道与发送频道解耦。
- [x] 使用 Session routing API 原子更新 `tx_group_id/rx_group_ids`。
- [x] App 退到后台后继续由前台服务维持自己的 Session，不受其他手机上线影响。
- [x] 移除 `JWTAuthGhostDeviceConflict` 作为新版 Android 同平台正常多端场景的等待逻辑。
- [x] legacy 服务端仍返回冲突时保留兼容提示。
- [x] 历史同步改用新的群组消息 API 和游标。
- [x] 使用结构化 sender 字段判断本人消息，不再依赖 `device_name` 和固定字符串解析。
- [x] 接收语音组装从单个全局 incoming stream 改为按 `source_group_id + sender identity` 隔离。
- [x] 多频道同时出现语音时，当前流实时播放，其他完整讲话流缓存并按到达顺序串行补播；消息未听和历史自动播放继续复用现有队列。
- [ ] 两台 Android 手机使用同一账号时均能在线、独立切换发送频道和订阅集合。
- [ ] 一台手机断线、切换网络或重认证不得导致另一台手机离线。

## 11. 其他原生客户端

- [ ] iOS、Windows、macOS 按与 Android 相同的实例 ID、Session、订阅和来源频道契约实现。
- [ ] PC 客户端确认是否存在多窗口/多进程场景，并使用安装实例 ID + 临时 Session 区分。
- [ ] 未升级客户端始终走 legacy 单实例槽位，不得与新版索引互相覆盖。
- [ ] 客户端资源分发中增加所需最小服务端协议版本和能力声明。

## 12. 观测、限流与运维

- [ ] 增加在线幽灵 Session 总数、按平台数量、按账号数量和 legacy/new 占比指标。
- [ ] 增加每 Session 接收订阅数和 fan-out 命中/去重数量指标。
- [ ] 增加会话上限拒绝、订阅上限拒绝、无效 session tag、地址不匹配和权限撤销计数。
- [ ] 增加消息 API 查询耗时、扫描行数、游标错误和音频授权失败指标。
- [ ] 日志使用 `session_id` 的短显示形式，不记录完整 JWT 或敏感会话凭证。
- [ ] 管理后台可查看账号的幽灵在线会话，但默认隐藏完整客户端实例标识和网络敏感信息。
- [ ] 提供管理员精确断开单个幽灵 Session 的能力。
- [ ] 增加配置开关，允许部署初期关闭幽灵多端或多频道订阅。
- [ ] 配置会话数、订阅数、消息查询频率和单页大小上限。
- [ ] 评估多频道订阅对 UDP fan-out 快照、WS 写队列、内存和带宽的放大影响。

## 13. 测试与验收矩阵

### 13.1 服务端单元测试

- [x] 同账号、同平台的两个新版幽灵 Session 可同时注册。
- [x] 同账号、不同平台的多个 Session 继续正常注册。
- [x] legacy 同账号同平台第二个幽灵连接仍按旧规则冲突。
- [x] 标准实体 UDP 设备第二个网络端点仍被拒绝。
- [x] 一个幽灵 Session 可以进入多个接收索引。
- [x] 更新订阅后旧索引完整清理，新索引完整建立。
- [x] 多订阅和互联路径不会造成重复投递。
- [x] 发送源只排除当前 Session，同账号其他端仍能收到。
- [x] 禁收、禁发、群组禁用和成员撤销立即生效。
- [x] 录音缓冲在多 Session、跨平台和跨通信域下不串流。
- [x] `go test -race` 已覆盖注册、断开、更新订阅、fan-out、迁移和控制链恢复并发路径。

### 13.2 API 集成测试

- [x] 消息列表返回频道内其他用户的语音和文本。
- [x] 通信记录默认仍只返回本人发信记录。
- [x] 消息详情和音频 URL 不能跨群组越权。
- [x] 私有群组成员退出后列表、详情和订阅全部返回 `403`。
- [x] 游标翻页期间插入新消息不产生遗漏或重复。
- [x] 多 Session routing 更新只影响指定 Session。
- [x] 多 Session 未指定 `session_id` 调旧切组接口返回明确歧义错误。
- [x] 删除一个 Session 不影响同账号其他 Session。

### 13.3 端到端场景

- [ ] 两台 Android 手机同账号、同为 SSID 101，同时在线。
- [ ] Android A 发送到群组 1，Android B 同时订阅群组 1 时可以收到。
- [ ] Android B 未订阅群组 1 时不会收到。
- [ ] Android A 订阅群组 1、2、3，收到消息后分别进入正确频道历史。
- [ ] Web 与两台 Android 同账号同时在线，任一端下线不影响其他端。
- [x] 服务端 E2E 已验证两个互联群组同时被订阅时，同一帧只投递一次；Android 场景仍待验收。
- [ ] 服务端按来源 Session/频道隔离流，Web 按来源流混音，Android 已实现按来源流串行排队；Android 双真机听感仍待验收。
- [x] 实体 UDP 设备尝试第二端上线时仍保持旧设备在线并拒绝新设备。
- [x] 中心与边缘各存在一个同平台幽灵 Session 时不会互相迁移或撤销。
- [ ] 网络切换、NAT 端口变化、JWT 刷新和服务重启后的行为符合会话恢复设计。

### 13.4 性能验收

- [x] 构造 1,000 个现代幽灵 Session、每 Session 4 个接收域的 fan-out 压力测试和 benchmark。
- [ ] 比较改造前后的 UDP fan-out 延迟、队列丢弃率、缓存构建耗时和内存占用。
- [ ] 验证单个热门频道不会因多订阅索引产生全局锁竞争。
- [x] 验证 WS 慢客户端不会阻塞其他 Session，并保持写队列有界。
- [x] 对百万级 `comm_records` 执行消息列表查询计划检查，并覆盖详情授权查询。
- [x] 验证消息 API 将游标 OR 分支拆为有界索引查询，不随页数增加线性退化。

## 14. 文档、迁移与发布

- [x] 更新 `docs/Protocol.md` 中幽灵设备认证、Reserved 字段、Session 和来源频道定义。
- [x] 更新 `docs/api/05-无线电与实时通信.md` 中会话与 routing API。
- [x] 更新 `docs/api/06-通联记录与日志.md`，区分通信记录和频道消息历史。
- [x] 更新 `docs/api/09-完整路由索引.md` 与错误码文档。
- [x] 更新 `docs/api/10-WebSocket协议详解.md`。
- [x] 更新节点互联协议及能力协商说明。
- [x] 编写数据库迁移、旧文本记录迁移和回滚说明。
- [x] 发布顺序采用“数据库兼容迁移 -> 中心兼容代码 -> 边缘节点 -> Web -> Android/原生客户端 -> 按能力覆盖率灰度”。
- [x] 在旧客户端占比较高时保持 legacy 单实例路径和旧 API。
- [ ] 统计新版能力覆盖率，达到发布门槛后再逐步废弃 `/api/radio/conflict` 和 comm-records 群组消息查询。
- [x] 回滚说明要求停止现代客户端入口并保留新会话偏好和消息字段，同时明确文本迁移后的数据兼容风险。

## 15. 推荐实施顺序

- [x] P0：修复通信记录详情越权、统一群组 ACL、修复幽灵录音缓冲键。
- [x] P1：新增频道消息 API、显式消息字段和游标分页。
- [x] P2：Web 与 Android 切换到频道消息 API，停止把通信记录接口当聊天历史接口。
- [x] P3：实现统一 GhostSession、实例偏好和会话 routing API。
- [x] P4：实现 WebSocket 多端与多频道订阅。
- [x] P5：扩展 UDP 协议、UDP 幽灵多端、来源频道元数据和 Android 多频道接收。
- [x] P6：扩展节点互联身份与边缘多 Session 路由。
- [ ] P7：服务端跨节点 E2E、竞态、1,000 Session 压力、百万消息计划、滚动升级文档及 Android 自动化门禁已完成；Android 双机/网络切换 E2E 待完成。
- [ ] P8：灰度开启多端，再灰度开启多频道订阅，最后清理 legacy 兼容代码。

## 16. 完成定义

- [ ] 同一账号的多台 Android 设备可以同时在线且互不覆盖。
- [ ] 同一账号的 Web、Android 和其他幽灵客户端可以同时在线且互不覆盖。
- [x] 服务端、Web 与 Android 的每个现代幽灵 Session 可以独立配置一个发送频道和多个接收频道。
- [x] 现代 Web 与 Android 实时消息均携带并解析真实来源频道；legacy 客户端继续按兼容语义处理。
- [x] Web 与 Android 按频道显示、缓存和同步其他用户的消息历史。
- [x] 通信记录与频道消息 API 的权限、返回模型和产品用途清晰分离。
- [x] 标准 UDP 实体设备仍严格保持单端在线。
- [ ] 中心、边缘和互联场景下均不存在会话覆盖、重复投递、串音或越权。
- [x] 旧客户端在兼容期可以继续使用原有单端单频道能力。
- [ ] 协议、API、数据库迁移、监控、测试和回滚文档全部完成。
