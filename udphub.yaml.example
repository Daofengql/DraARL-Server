# NRL Link 配置文件模板
# 复制此文件为 udphub.yaml 并根据实际情况修改配置

# 系统配置
System:
    Host: ""                   # 监听地址，空则监听所有网卡
    Port: "60050"              # UDP服务端口
    LogPath: ""                # 日志文件路径，空则不输出文件
    IPfile: ./udphub.ipdb      # IP数据库文件路径

# MySQL数据库配置
Database:
    Host: "127.0.0.1"
    Port: 3306
    User: "nrllink"
    Password: "your_password_here"
    DBName: "nrllink"
    Charset: "utf8mb4"
    Collate: "utf8mb4_unicode_ci"

    # 连接池配置
    MaxOpenConns: 25           # 最大打开连接数
    MaxIdleConns: 5            # 最大空闲连接数
    MaxLifetime: 300           # 连接最大存活时间(秒)

# Redis 配置 (必需，用于三级缓存)
Redis:
    Host: "127.0.0.1"
    Port: 6379
    Password: ""               # Redis密码
    DB: 0                      # 数据库编号
    PoolSize: 100              # 连接池大小
    MinIdleConn: 10            # 最小空闲连接数

    # 缓存配置
    Cache:
        LocalTTL: 60           # 本地缓存TTL(秒)
        RedisTTL: 3600         # Redis缓存TTL(秒)
        MaxSize: 10000         # 本地缓存最大条目数

# Web服务配置
Web:
    Host: ""                   # 监听地址，空则监听所有网卡
    Port: "9000"               # Web服务端口

# Keycloak SSO 单点登录配置 (可选)
Keycloak:
    Enabled: false                    # 是否启用SSO登录
    BaseURL: "http://localhost:8080"   # Keycloak服务器地址
    Realm: "nrllink"                  # Realm名称
    ClientID: "nrllink-frontend"      # 客户端ID
    ClientSecret: ""                  # 客户端密钥(如需要)
    RedirectURI: "http://localhost:9000/callback"  # OAuth回调地址
    AdminUsername: "admin"            # 管理员用户名(用于同步用户)
    AdminPassword: "admin"            # 管理员密码(用于同步用户)

# MinIO 对象存储配置 (可选)
MinIO:
    Endpoint: "localhost:9000"        # MinIO服务地址
    AccessKey: "minioadmin"           # Access Key
    SecretKey: "minioadmin"           # Secret Key
    UseSSL: false                     # 是否使用HTTPS
    Bucket: "nrllink"                 # 默认存储桶
    BasePath: "/minio"                # URL访问基础路径
