# 幽灵设备多端在线、多收单发与频道消息历史改造 TODO

> 本文替换旧 `TODO.md`，完整记录需求原意、设计结论、当前实现状态、剩余工作和验收标准。
>
> 状态说明：`[x]` 表示已经实现并完成相应自动化验证；`[ ]` 表示尚未完成，或虽有代码但还没有达到可交付验收标准。
>
> 状态快照：2026-08-06。

## 1. 需求原意

当前系统原本基本只能“单听、单发”：一个设备只有一个当前群组，该群组同时决定发送目标和接收来源；通信列表（包括 Android 客户端）看起来又主要按本人设备查询，因此用户容易只能看到自己设备产生的消息记录，而不能按频道查看完整消息历史。

本次改造希望实现以下产品形态：

- 特殊幽灵设备取消单端限制，允许同一账号在多台手机、多个浏览器或其他客户端上同时在线。
- 多端在线只属于特殊幽灵设备。标准 UDP 实体设备仍然只能单端在线，继续使用现有身份唯一性、在线冲突和旧连接保护规则。
- 幽灵设备支持“多收单发”：一个在线会话可以同时接收多个频道/群组的语音与文本，但发送时只能选择一个明确的目标频道。
- 同一账号的不同幽灵客户端彼此独立。一台移动设备上线、重连、切组或下线，不能覆盖、踢掉或修改其他移动设备的会话。
- 消息历史使用独立 API，不继续把通信记录接口同时当作聊天记录接口。
- 消息历史与通信记录可以继续来源于同一张 `comm_records` 表，但必须采用不同的查询条件、权限边界和响应模型。
- 通信记录继续表示“本人设备发信记录、通信审计和管理员日志”；消息历史则按用户有权访问的频道/群组查询，返回该频道内所有可见发送者的消息。
- Web、Android 和后续客户端应能按群组查询完整历史，而不是只能查询当前账号设备的历史。这也为消息日志、游标同步、语音回放和后续检索提供稳定基础。

这项需求不能通过简单删除“幽灵设备已在线”的冲突判断完成。系统必须明确区分账号、平台、客户端安装实例、临时在线会话、唯一发送频道和多个接收订阅，同时保证实体设备的单端规则完全不变。

## 2. 分析结论与硬性边界

### 2.1 可行性结论

- [x] 方案在现有架构上可行，不需要为消息历史另建一套消息主表。
- [x] 幽灵设备已经从 `owner_id + SSID` 单值在线身份拆分为独立 `GhostSession`。
- [x] 实体设备与幽灵设备继续走两套明确的在线身份语义，实体设备未被纳入多 Session 模型。
- [x] “多收单发”已经落在每个 Session 的 `tx_group_id + rx_group_ids` 上，而不是账号全局状态。
- [x] 频道消息 API 已从通信记录 API 中拆分，并继续复用 `comm_records` 数据源。
- [ ] 生产完成仍取决于观测、灰度开关、部署限流、管理页面和多真机验收。

### 2.2 必须长期保持的不变量

- [x] 标准 UDP 实体设备继续以 `owner_id + ssid` 作为唯一运行时身份。
- [x] UDP `DraARLv1` 包结构、90 字节头、字段偏移和实体设备编解码定义保持不变；新增能力只解释既有 Reserved 字段，不新增、移动或扩展字段。
- [x] 实体设备的第二个网络端点仍触发冲突保护，不能因幽灵多端而放宽。
- [x] UDP 幽灵设备 `101-104` 与 WebSocket 幽灵设备 `105` 使用 Session 模型。
- [x] 每个幽灵 Session 只能拥有一个 `tx_group_id`。
- [x] 每个现代幽灵 Session 可以拥有多个去重后的 `rx_group_ids`。
- [x] `tx_group_id` 必须包含在 `rx_group_ids` 中。
- [x] 上行媒体的发送目标只取服务端 Session 的 `tx_group_id`，不能信任客户端在媒体包里声明任意目标群组。
- [x] 下行消息必须保留真实 `source_group_id`，客户端不得用当前选中群组猜测消息来源。
- [x] 同账号其他 Session 不属于“发送源”，应该可以正常收到当前 Session 发出的消息。
- [x] 一个 Session 经多个订阅或互联路径命中时只投递一次。
- [ ] 接收权限与发送权限需要进一步拆成独立授权模型；目前“可查看群组”仍承担了大部分路由授权语义。
- [ ] 是否增加账号级语音话权锁仍需产品决策；当前“单发”保证的是每 Session 单一发送频道，不等于同账号全局只能有一个 Session 发言。

## 3. 领域模型与术语

- [x] `client_instance_id`：客户端安装实例的稳定随机 UUID，用于区分同账号的不同手机、桌面安装或浏览器 profile；不得使用 IMEI、MAC、Android ID 等硬件指纹。
- [x] `session_id`：服务端为一次在线连接签发的临时、不可预测标识；重连可生成新值。
- [x] `session_tag`：UDP 热路径定位已认证 Session 的短标识；它不是认证凭据，必须与账号、平台、端点和 Session 状态联合校验。
- [x] `tx_group_id`：该 Session 当前唯一发送频道。
- [x] `rx_group_ids`：该 Session 当前直接订阅的接收频道集合。
- [x] `source_group_id`：消息实际产生时的源频道。
- [x] `requested_group_id`：用户查询或正在查看的频道；存在互联关系时可能与源频道不同。
- [x] `legacy session`：没有 `client_instance_id` 或不声明新能力的旧客户端，同账号同平台仍进入单实例、单频道兼容槽位。
- [x] `modern session`：提交稳定实例 ID，并声明 Session、多收和来源频道能力的新版客户端。

## 4. P0：安全基线与旧问题修复

### 4.1 通信记录授权

- [x] 为通信记录详情增加记录级授权，禁止普通用户通过递增 ID 获取其他用户录音。
- [x] 普通用户只能查询本人实体设备或本人幽灵设备产生的通信记录。
- [x] 管理员审计模式保留全局通信记录查询能力。
- [x] 只有授权成功后才生成 MinIO/S3 音频 URL。
- [x] 频道消息详情不复用“本人通信记录”权限，而是使用频道 ACL。
- [x] 覆盖本人、其他用户、管理员、无权限和不存在记录的授权测试。

### 4.2 群组访问规则

- [x] 消息列表、消息详情和会话 routing 更新复用统一群组查看权限。
- [x] 无效群组 ID 返回 `400`，不存在返回 `404`，无权限返回 `403`。
- [x] 私有群组仅允许管理员、群主或有效成员访问。
- [x] 群组禁用、删除或成员权限撤销后会重新整理或断开受影响的在线 Session。
- [ ] 设计单独的 `canTransmitGroup`/发送成员权限，避免长期把“可以查看”直接等同为“可以发言”。
- [ ] 为发送权限增加后台配置、成员角色和权限变更 UI。

### 4.3 幽灵录音缓冲隔离

- [x] 将运行时录音源键与最终持久化的 `device_id` 分离。
- [x] 实体设备继续使用稳定实体设备 ID 作为录音来源。
- [x] Web/UDP 幽灵设备使用带 Session 身份的录音源键，避免 `device_id=0` 或同账号多端串流。
- [x] 持久化时幽灵消息仍保存 `device_id=0`、实际 `user_id`、平台 SSID 和发送者快照。
- [x] 已覆盖不同 Session、不同频道并发录音不合并、不覆盖的测试。

## 5. P1：拆分频道消息历史 API

### 5.1 API 边界

- [x] 保留 `GET /api/comm-records` 作为本人发信记录/设备审计接口。
- [x] 保留管理员通信记录查询语义。
- [x] 新增 `GET /api/groups/:group_id/messages`，按频道 ACL 获取消息历史。
- [x] 新增 `GET /api/groups/:group_id/messages/:message_id`，获取单条消息及授权后的音频地址。
- [x] 消息 API 返回频道内所有可见发送者，不按当前用户设备过滤。
- [x] 普通消息流只返回已完成记录 `status=2`。
- [x] 支持 `message_type=all|voice|text`。
- [x] 支持 `limit`，当前范围为 `1-100`，默认 `50`。
- [x] 使用包含 `start_time + id` 的不透明游标，保证相同时间戳下稳定翻页。
- [x] 返回 `next_cursor`、`has_more` 和 `server_time`。
- [x] 消息详情再次执行频道授权，不允许仅凭消息 ID 跨频道获取音频 URL。
- [ ] 为消息 API 增加部署级单页上限配置，避免上限长期硬编码。
- [ ] 为消息 API 增加按账号/IP 的查询频率限制和明确的 `429` 响应。
- [ ] 制定旧 `/api/comm-records?group_id=...` 群组消息用法的废弃时间表。

### 5.2 独立消息响应模型

- [x] 使用独立 `MessageResponse`，不直接暴露 `CommRecordResponse`。
- [x] 返回 `id`、`message_type`、`source_group_id` 和 `requested_group_id`。
- [x] 返回发送时间、结束时间、语音时长和消息状态。
- [x] 返回结构化 sender：`user_id`、`username`、`callsign`、`nickname`、`ssid`、`dev_model`、`is_ghost`。
- [x] 文本消息返回 `text_content`。
- [x] 语音消息返回授权后的 `audio_url`、`audio_size` 和音频格式。
- [x] 时间统一使用 RFC 3339 UTC。
- [x] 客户端无需再解析 `device_name` 字符串来推断发送者或 SSID。

### 5.3 同表数据模型与迁移

- [x] 继续使用 `comm_records` 作为语音和文本消息的共同事实来源。
- [x] 增加显式 `message_type`，不再长期依赖 `audio_path` 的 `text:` 前缀。
- [x] 增加独立 `text_content`，避免文本受音频路径字段长度和语义限制。
- [x] 增加发送时的用户名、呼号、昵称和设备型号快照。
- [x] 保留旧 `text:` 记录兼容读取，并提供分批回填迁移。
- [x] `group_id` 在消息领域中明确作为 `source_group_id` 使用。
- [x] 增加 `(group_id, status, start_time, id)` 复合游标索引。
- [x] 已对百万行数据执行查询计划和游标性能验证。
- [ ] 单独压测 `message_type` 筛选后决定是否增加包含类型的复合索引，避免未经验证增加写放大。
- [ ] 大表生产迁移前在真实数据副本上记录 DDL 持锁时间、磁盘增量和回滚窗口。

### 5.4 互联群组历史

- [x] 第一版按当前启用的互联拓扑计算可见消息集合。
- [x] 请求频道自身必须先通过 ACL，不能通过互联关系绕过私有频道权限。
- [x] 同一条记录经多个互联路径可见时只返回一次。
- [x] 响应保留真实 `source_group_id`。
- [ ] 如果产品要求严格还原发送当时的投递范围，增加不可变通信域/投递范围快照；当前实现不能用后来变化的互联拓扑还原历史事实。

## 6. P2：统一 Ghost Session 与偏好模型

### 6.1 Session 注册表

- [x] 建立统一 `GhostSession` 领域模型和并发安全注册表。
- [x] Session 包含实例、账号、平台、SSID、传输、协议、能力、活动时间、收发权限和 routing。
- [x] 同账号、同平台的不同现代 `client_instance_id` 可以同时注册。
- [x] 同一实例重连只替换自己的旧 Session，不影响同账号其他实例。
- [x] legacy 同账号同平台第二个连接继续冲突。
- [x] Session 清理使用 `session_id`，旧连接延迟退出不能误删重连后的新连接。
- [x] 每账号 Session 上限可配置，默认 `8`。
- [x] 每 Session 订阅上限可配置，默认 `16`。
- [x] 达到上限返回实际数量和限制，不随机踢掉其他客户端。
- [ ] 增加多端功能总开关及按账号/平台灰度开关。
- [ ] 增加多收功能总开关及按账号/平台灰度开关。

### 6.2 实例偏好持久化

- [x] legacy 客户端继续使用 `user_device_preferences(user_id, dev_model)` 保存单一频道。
- [x] 现代客户端按 `user_id + dev_model + client_instance_id` 保存实例 routing 偏好。
- [x] 每个实例持久化最后 `tx_group_id` 和多个 `rx_group_ids`。
- [x] 首次升级以原平台最后频道初始化发送和首个接收频道。
- [x] 偏好不是在线状态，服务启动不会把偏好记录恢复成在线 Session。
- [x] routing 更新逻辑上原子化；持久化或运行时投影失败会回滚。
- [x] 用户、群组和权限变化会触发偏好/在线 routing 整理。

### 6.3 用户会话 API

- [x] `GET /api/radio/sessions` 仅返回当前账号的在线幽灵 Session。
- [x] 返回平台、实例、传输、在线时间、活动时间、发送频道、接收集合和禁收禁发状态。
- [x] `PUT /api/radio/sessions/:session_id/routing` 原子更新指定 Session 的 `tx_group_id/rx_group_ids`。
- [x] 更新前一次性验证群组存在、状态、类型、权限和订阅上限。
- [x] `DELETE /api/radio/sessions/:session_id` 只断开当前账号拥有的指定 Session。
- [x] 旧切组 API 在同平台存在多个匹配 Session 且未指定 `session_id` 时返回 `409 ambiguous_session`。
- [x] 旧客户端继续保持单频道 routing。

## 7. P3：UDP 幽灵设备多端与多收

### 7.1 认证和线协议

- [x] 幽灵 JWT 认证支持版本化 payload、`client_instance_id`、协议版本和能力集合。
- [x] 兼容旧版“DATA 仅为 JWT 字符串”的认证请求。
- [x] 认证成功响应返回 Session 信息、`session_tag`、初始发送频道和接收集合。
- [x] 后续上行心跳、语音和文本包携带 `session_tag`。
- [x] 下行语音/文本包携带真实 `source_group_id`。
- [x] 旧客户端仍进入 legacy 单实例、单频道路径。
- [x] 非法 tag、账号/SSID/平台不匹配、网络端点不匹配均拒绝。
- [x] NAT 地址变化要求重认证，不允许仅凭 username/SSID 抢占 Session。
- [ ] 在协议文档中补充 tag 猜测、重放窗口和未来安全重绑定方案的威胁模型。

### 7.2 UDP 运行时和 fan-out

- [x] UDP 幽灵设备按 Session 管理，并提供 tag 和实例重连索引。
- [x] 接收索引按 `group/domain -> session` 维护，一个 Session 可进入多个频道索引。
- [x] routing 更新一次完成旧索引移除和新索引加入。
- [x] 上行只使用 Session 的唯一 `tx_group_id`。
- [x] fan-out 根据 `rx_group_ids` 和当前通信域选择目标。
- [x] 发送源排除精确到当前 Session，同账号其他移动端可以接收。
- [x] 同一 Session 因多个订阅或互联路径命中时去重。
- [x] 禁收、会话失效和权限撤销的 Session 不进入有效接收快照。
- [x] UDP 超时、主动下线、JWT 失效和实例重连按 Session 精确清理。
- [x] 标准实体设备运行时索引和冲突规则保持原样。
- [ ] 补充改造前后热门频道 fan-out 延迟、缓存构建时间、队列丢弃率、内存和锁竞争对比报告。

## 8. P4：WebSocket 与 Web 客户端

### 8.1 WebSocket 服务端

- [x] 取消现代幽灵客户端“同一账号已有设备在线即 409”的单端限制。
- [x] WebSocket 幽灵连接按 `session_id` 管理，并维护账号到 Session 集合的辅助索引。
- [x] 断开、心跳超时和写队列失败只清理当前 Session。
- [x] 群组接收索引按 Session 的多个订阅维护。
- [x] 下行语音与文本携带 `source_group_id`。
- [x] 多订阅和互联路径按 Session 去重。
- [x] legacy Web 客户端保持单实例、单频道兼容。

### 8.2 Web 界面

- [x] 生成并持久化 Web `client_instance_id`。
- [x] 认证后保存当前连接的 `session_id`。
- [x] 提供唯一发送频道选择和多个收听频道选择。
- [x] 显示各来源频道，并支持按频道开关与音量控制。
- [x] 实时消息按真实 `source_group_id` 分流。
- [x] 多个频道语音按来源隔离并可并发混音。
- [x] 历史消息切换到频道消息 API 和游标分页。
- [ ] 决定同一浏览器 profile 的多标签页语义。当前共享安装级实例 ID，新标签页会替换旧标签页；如需并存应增加标签页实例 ID 或改用 `sessionStorage`。
- [ ] 对慢 WebSocket 客户端、后台标签页节流和长时间休眠恢复补浏览器实测。

## 9. P5：中心、边缘与互联路由

- [x] 实体设备互联身份继续保持 `owner_id + ssid`。
- [x] 幽灵设备互联身份扩展到实例/Session，第二个同平台 Session 不会撤销第一个。
- [x] 节点授权、active identity、Session epoch、确认和撤销支持幽灵 Session 身份。
- [x] 上行 Relay 使用 Session 的 `tx_group_id/domain_id`。
- [x] 下行 Relay 按接收订阅投递，并在多路径命中时去重。
- [x] 节点重连、批量确认、控制链恢复和 Session 撤销可精确处理单个 Session。
- [x] 新旧节点协议保持能力兼容路径。
- [x] 已覆盖中心直连、边缘直连、跨边缘同账号多端、控制链恢复和重复投递测试。
- [ ] 在真实滚动升级环境执行“中心先升级、边缘后升级、最后开功能”的演练。
- [ ] 验证中心重启、边缘抖动和长时间分区恢复后没有幽灵 Session 泄漏。

## 10. P6：Android 客户端

- [x] 生成并持久化随机 Android `client_instance_id`。
- [x] 扩展 UDP JWT 认证请求和成功响应解析。
- [x] 上行心跳、文本和语音携带 `session_tag`。
- [x] 解析下行包的真实 `source_group_id`。
- [x] 本地消息按账号和真实来源频道隔离。
- [x] 提供唯一发送频道和多个接收频道设置。
- [x] 使用 Session routing API 原子更新发送/接收集合。
- [x] 新版 Android 不再把同平台幽灵连接视作正常冲突场景。
- [x] 兼容旧服务端返回的 legacy 冲突提示。
- [x] 历史同步切换到频道消息 API 和游标。
- [x] 使用结构化 sender 判断来源和本人消息。
- [x] 接收语音按来源频道与发送者隔离，并使用完整讲话流队列处理并发语音。
- [x] Android 单元测试、lint 和 debug APK 构建通过。
- [ ] 两台 Android 真机使用同一账号完成同时在线、互不覆盖验收。
- [ ] 验证 App 前后台切换、系统休眠、网络切换和重新认证不会影响另一台设备。
- [ ] 用真实语音验证多频道并发时的可懂度、排队时延、未听状态和回放体验。

## 11. 其他客户端与兼容策略

- [ ] iOS 按 Android 的实例 ID、Session、tag、routing 和来源频道契约适配。
- [ ] Windows/macOS 客户端按相同契约适配。
- [ ] PC 客户端明确多窗口/多进程是共享安装实例还是独立实例。
- [x] 未升级客户端继续进入 legacy 单实例、单频道槽位。
- [ ] 客户端资源发布中声明所需最小服务端版本和协议能力。
- [ ] 统计 legacy/modern 客户端占比和版本覆盖率。
- [ ] 覆盖率达到门槛后再移除 legacy `Remove(username, ssid)`、冲突接口和旧群组消息查询路径。

## 12. P7：观测、管理、限流与隐私

### 12.1 Session 指标

- [x] 导出在线幽灵 Session 数、在线账号数、平台分布和传输类型分布。
- [x] 在线设备总数按实体设备唯一键与幽灵 Session 分开统计；多收订阅不会重复增加总数。
- [x] 群组在线数按该群组的物理设备和去重后的幽灵 Session 计算，平台总览与群组列表补上运行时幽灵设备。
- [x] 导出 legacy/modern 占比、总订阅数、最大订阅数和配置上限。
- [x] 统计注册、实例替换、清理、实例冲突、会话上限、订阅上限和权限撤销。
- [x] 统计 UDP 无效 tag、身份不匹配、端点不匹配和注册表拒绝。
- [x] 增加 UDP/WS fan-out 候选数、跨路径去重数、写队列丢弃和发送失败指标。
- [x] 增加消息 API 请求量、累计耗时直方图、返回条数、游标错误、数据库错误和音频授权成功/失败指标。
- [x] `/api/udp/metrics` 由 Admin 中间件保护，并记录各层稳定字段含义。
- [ ] 建立生产监控面板并确定 Session 上限拒绝、包拒绝、消息 5xx 和延迟分位告警阈值。

### 12.2 管理能力

- [x] 提供管理员 Session 摘要列表 API。
- [x] 提供管理员精确断开单个幽灵 Session 的 API，并记录操作日志。
- [x] 管理 API 默认隐藏完整 `client_instance_id`、`session_tag`、JWT 和网络端点。
- [x] 增加后台 Session 管理页面，支持按账号、平台、传输和 legacy 状态筛选。
- [x] 断开操作增加确认、成功/失败反馈和自动/手动刷新。
- [x] 当前权限统一使用 `RequireAdmin`；系统没有独立超级管理员断开权限。

### 12.3 日志和隐私

- [x] 所有幽灵会话日志统一只显示 `session_id` 八字符短形式。
- [x] 清理 WebSocket、UDP 和 routing 整理日志中的完整 Session/实例标识。
- [x] 不记录完整 JWT。
- [x] 幽灵认证与会话日志不再记录客户端完整网络端点；实体设备冲突诊断仍保留原有端点日志。
- [ ] 为 Session 管理与强制断开增加审计查询和留存策略。

### 12.4 部署限额与灰度

- [x] 每账号 Session 数和每 Session 订阅数可配置。
- [ ] 增加“允许现代幽灵多端”总开关。
- [ ] 增加“允许现代幽灵多收”总开关。
- [ ] 支持按账号、平台或租户灰度，而不只是全局同时开启。
- [ ] 配置消息 API 单页上限、查询频率和并发上限。
- [ ] 所有限额拒绝返回稳定错误码，并在客户端提供可操作提示。

### 12.5 npm 依赖维护

- [x] 使用锁文件重新解析补丁版本，修复 `brace-expansion` 和 `postcss` 审计漏洞。
- [x] 将 `react-router-dom` 更新到 `7.18.2`，保持 v7 API，不执行 `npm audit fix --force`。
- [x] 将 `eslint-plugin-react-hooks` 固定为已验证的 `7.0.1`，避免工具升级把既有代码误报为 React Compiler 规则错误。
- [x] `npm ci`、lint 和生产构建通过；审计仅剩当前 v7 无可用修复版本的 RSC Mode CSRF 告警，项目未使用 RSC。
- [ ] 等待 React Router 发布包含 RSC 修复的兼容版本后再次升级并清除审计例外。

## 13. 账号级单发/话权决策

当前已经实现“每个 Session 只有一个发送频道”，但同一账号的两个 Session 理论上仍可能分别尝试发言。上线前必须明确产品定义：

- [ ] 决定是否需要账号级语音 PTT 锁。建议启用，确保同一账号同一时刻只有一个幽灵 Session 占用语音话权。
- [ ] 决定文本是否受账号级话权锁限制。建议文本不占用语音锁，但仍只能发往所属 Session 的 `tx_group_id`。
- [ ] 如果启用，定义租约、超时、续租、断线释放和服务重启行为。
- [ ] 固定账号锁与通信域半双工锁的获取顺序，避免死锁和部分占用。
- [ ] 任一锁获取失败时释放已获得的另一层锁。
- [ ] PTT 期间禁止切换 `tx_group_id`，或定义明确的停止当前 PTT 后切换语义。
- [ ] 增加同账号双端、不同账号同域、同账号不同域和互联域并发测试。

## 14. 自动化测试与性能验收

### 14.1 已完成自动化门禁

- [x] 服务端 `go test ./...`。
- [x] 服务端 `go test -race ./... -count=1`。
- [x] 服务端 `go vet ./...`。
- [x] MySQL 通信记录、消息历史、Session、边缘路由、偏好迁移、百万行消息和客户端资源 E2E。
- [x] 1,000 个现代 Session fan-out 与跨边缘多 Session 测试。
- [x] Web lint 和 production build。
- [x] MkDocs strict build。
- [x] Android test、lintDebug 和 assembleDebug。

### 14.2 当前观测阶段完成前必须重跑

- [x] 格式化并编译当前 Session 指标与管理 API 改动。
- [x] 为指标快照、拒绝计数、管理员列表和强制断开补齐单元测试。
- [x] 在 Ubuntu VM 执行目标包测试、全量测试、race、vet 和全部 MySQL E2E。
- [x] 验证管理响应不泄露完整客户端实例、tag 或端点。
- [ ] 完成后按独立阶段提交，不与后续灰度开关混为同一提交。

### 14.3 性能与容量

- [x] 百万行频道消息游标查询使用预期复合索引，不随页码线性退化。
- [x] 1,000 Session 基础 fan-out 场景已覆盖。
- [ ] 使用部署预期的“每账号 Session 数 × 每 Session 订阅数 × 热门频道人数”计算最坏接收放大倍数。
- [ ] 对改造前后 UDP fan-out 延迟、缓存时间、内存、锁竞争和丢包率形成对比报告。
- [x] 验证 WebSocket 慢客户端不会阻塞其他 Session，且写队列保持有界。
- [ ] 对 `message_type` 筛选执行独立百万行查询计划和耗时验证。

## 15. 真机与端到端验收矩阵

- [ ] 两台 Android 真机使用同一账号、同为幽灵 SSID `101`，可以同时在线且互不覆盖。
- [ ] Android A 发往频道 1，Android B 订阅频道 1 时能够收到。
- [ ] Android B 取消订阅频道 1 后不再收到频道 1 的实时消息。
- [ ] Android 同时订阅频道 1、2、3，文本、语音和历史均进入真实来源频道。
- [ ] Web 与两台 Android 同账号同时在线，任一端下线、切组或重连不影响其他端。
- [ ] 一台 Android 在 Wi-Fi/蜂窝间切换并重新认证时，另一台持续在线。
- [ ] 多个频道同时出现真实语音时，验收 Web 混音与 Android 队列策略。
- [ ] 标准 UDP 实体设备尝试从第二端上线时，继续按原规则拒绝冲突端。
- [ ] 中心与边缘各存在一个同平台幽灵 Session 时不会互相迁移或撤销。
- [ ] 中心重启、边缘重连和滚动升级期间无 Session 覆盖、重复投递、串音或越权。
- [ ] 私有群组成员被移除后，历史查询、实时接收和发送能力及时失效。

## 16. 文档、迁移与发布

- [x] 更新无线电/实时通信 API 文档。
- [x] 更新通信记录与频道消息历史边界文档。
- [x] 更新 WebSocket 协议、UDP Session/tag 和来源频道说明。
- [x] 编写幽灵多端与频道消息数据库升级说明。
- [x] 记录 `comm_records` 字段、回填和复合索引迁移步骤。
- [x] 补充观测指标、后台 Session 管理和强制断开操作手册。
- [ ] 补充多端/多收开关、查询限流和紧急关闭流程。
- [ ] 在生产数据副本完成迁移演练并记录实际耗时。
- [ ] 执行发布顺序：数据库兼容迁移 -> 兼容服务端 -> 中心 -> 边缘 -> Web -> Android/原生客户端 -> 开启灰度。
- [ ] 先灰度开启幽灵多端，再单独灰度开启多收。
- [ ] 回滚时只关闭新入口，不删除新字段、偏好和索引，保证可重新启用。
- [ ] 新版能力覆盖率达到门槛后，再计划清理 legacy 路径。

## 17. 推荐后续实施顺序

- [x] P7.1：完成 Session/UDP/消息指标、管理员 Session API、短 ID 日志和隐私清理。
- [x] P7.2：增加管理后台 Session 页面。
- [ ] P8.1：实现多端、多收灰度开关和稳定错误码。
- [ ] P8.2：实现消息 API 单页配置、查询限流和查询指标。
- [ ] P9：完成 Ubuntu 全量自动化、性能对比和生产迁移演练。
- [ ] P10：完成双 Android 真机、Web + Android、NAT/切网和真实语音 E2E。
- [ ] P11：决定并实现账号级 PTT 锁与独立发送权限模型。
- [ ] P12：灰度发布、观察指标、处理兼容问题，达到门槛后清理 legacy。

## 18. 最终完成定义

- [ ] 同一账号的多台 Android 幽灵设备可以同时在线，彼此不覆盖。
- [ ] 同一账号的 Web、Android 和其他幽灵客户端可以同时在线，彼此不覆盖。
- [ ] 每个幽灵 Session 可以独立配置一个发送频道和多个接收频道。
- [ ] 所有实时消息都携带真实来源频道，并按 Session 精确投递和去重。
- [ ] Web 与 Android 可以按频道显示、缓存和同步其他发送者的完整消息历史。
- [ ] 通信记录与频道消息 API 的权限、响应和产品用途清晰分离。
- [ ] 标准 UDP 实体设备仍严格保持单端在线。
- [ ] 中心、边缘和互联场景不存在 Session 覆盖、重复投递、串音或越权。
- [ ] 管理、指标、日志隐私、限流和紧急开关满足生产运维要求。
- [ ] 旧客户端在兼容期继续使用原有单端、单频道能力。
- [ ] 双真机、真实网络、真实语音、滚动升级和回滚演练全部通过。

当前结论：核心架构和主要客户端能力已经落地，用户提出的“幽灵设备多端在线 + 多收单发 + 按群组查询消息历史”在技术上已经成立；剩余工作集中在生产观测与管理、灰度/限流、账号级发送规则、其他客户端适配和真实设备验收。只有第 18 节全部满足后，才能把该功能视为生产完成。
