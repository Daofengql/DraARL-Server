# 幽灵设备多端、多收单发与频道消息 TODO

> 本文已删除完成项的逐条流水账，只保留原始设想、已交付边界、验收证据和真正剩余工作。
> 最近同步：2026-08-06。

## 用户原始设想

当前系统原本基本只能“单听、单发”，通信列表及 Android 客户端看起来也只能看到自己设备的消息记录。期望实现以下产品形态：

- 特殊幽灵设备取消单端限制，同一账号允许多台移动设备、浏览器或其他客户端同时在线。
- 多端能力只属于特殊幽灵设备；标准 UDP 实体设备仍严格保持单端在线，继续执行原有冲突和唯一在线约束。
- 幽灵设备支持“多收单发”：每个在线会话可以同时接收多个频道/群组，但发送时只能选择一个明确的目标频道。
- 同账号的不同幽灵客户端会话彼此独立；一个客户端上线、切组、重连或下线不能覆盖其他客户端。
- 消息历史使用专用 API，不再把通信记录接口当作聊天记录接口。
- 消息和通信记录可以继续来源于同一张表，但使用不同查询条件、权限语义和响应模型。
- 通信记录按本人设备或管理员审计语义查询；消息历史按有权访问的频道/群组查询。
- Web、Android 和后续客户端能按群组查看其他发送者的完整消息历史，也便于消息日志、分页同步和语音回放。

## 已交付边界

本次核心改造已经完成自动化验收：

- 标准 UDP 实体设备继续以 `owner_id + ssid` 保持单端在线；幽灵设备才进入多 Session 模型。
- WebSocket `105` 与 UDP 幽灵设备 `101-104` 支持稳定安装实例 ID、临时 `session_id` 和 UDP `session_tag`。
- 每个现代幽灵 Session 独立保存一个 `tx_group_id` 和多个 `rx_group_ids`，且发送频道始终包含在接收集合中。
- 旧客户端没有 `client_instance_id` 时继续进入 legacy 单实例槽位。
- 切组已升级为 Session routing 原子更新；旧切组 API 遇到多个匹配 Session 时返回明确歧义错误。
- Web 已提供收听频道选择、按频道开关/音量控制、来源流隔离和并发语音混音。
- UDP、WebSocket、中心/边缘互联均携带和保留真实 `source_group_id`，并按 Session 精确排除发送源、按通信域去重。
- Android 已接入版本化 UDP 认证、安装实例 ID、Session tag、多收路由、来源频道、频道消息历史及多来源语音队列。
- 新增 `GET /api/groups/:group_id/messages` 与消息详情 API；通信记录与频道消息的用途、权限和返回模型已分离。
- `comm_records` 已增加显式消息类型、文本内容、发送者快照和频道消息游标索引，保留旧文本兼容迁移。
- 幽灵录音缓冲按唯一 Session 隔离，不再因 `device_id=0` 或同账号多端发生串流。
- 每账号 Session 数和每 Session 订阅数已通过 `GhostSessions` 配置，默认分别为 `8` 和 `16`，中心与边缘统一校验。
- 达到 Session/订阅上限时返回实际数量和上限，不会随机踢出其他移动设备。

主要本地提交：

- 服务端：`f5f4d43` 至 `ff7dca2`，按安全基线、消息 API、Session 基础、WS/Web、UDP、节点互联、性能与配置阶段分别提交。
- Android：`5c9f645` 至 `d9a8fbd`，按认证协议、路由/历史、语音流隔离与并发队列分别提交。

## 自动化验收证据

### 服务端与 Web

Ubuntu Hyper-V VM 使用当前服务端提交 `ff7dca2` 的独立源码副本完成：

- `go test ./...`
- `go test -race ./... -count=1`
- `go vet ./...`
- 开启 `DRAARL_COMM_RECORDS_E2E`、`DRAARL_MESSAGES_E2E`、`DRAARL_RADIO_SESSIONS_E2E`、`DRAARL_EDGE_GHOST_E2E`、`DRAARL_GHOST_PREFERENCE_E2E`、`DRAARL_MESSAGE_MIGRATION_E2E`、`DRAARL_MESSAGE_PERFORMANCE_E2E` 和 `DRAARL_CLIENT_RESOURCE_E2E` 的全量 MySQL 测试。
- 百万行频道消息游标查询计划、1,000 个现代 Session fan-out、跨边缘多 Session、控制链恢复和去重测试。
- Web `npm ci`、`npm run lint`、`npm run build`。
- `mkdocs build -f docs/mkdocs.yml --strict`。

### Android

Android `d9a8fbd` 已完成独立 VM 构建；2026-08-06 又在允许的本地环境执行：

- `gradlew test lintDebug assembleDebug --no-daemon`
- 53 个 Gradle task 成功，生成 44 份测试 XML。
- lint HTML/SARIF 报告成功生成。
- debug APK 成功生成，大小 `51,294,336` 字节。

这些证据覆盖编译、单元测试、静态检查、协议、路由、历史同步和打包，不替代双真机、网络切换和真实音频听感验收。

## 剩余产品与安全决策

- [ ] 设计独立的发送权限与接收权限，不能长期把“可查看群组”等同于“可在群组发言”；需要先确定群组成员/管理员的数据模型和管理 UI。
- [ ] 决定是否增加账号级语音话权锁。建议同一账号同一时刻只允许一个幽灵 Session 发起 PTT，避免两台移动设备同时发言。
- [ ] 明确文本是否受账号级话权锁限制。建议文本不占用语音 PTT 锁，但仍只能发往当前 `tx_group_id`。
- [ ] 如果启用账号级 PTT 锁，定义租约、超时、锁获取顺序、失败回滚、切组冲突和断线释放，并补同账号/不同账号/互联域并发测试。

## 剩余数据与历史设计

- [ ] 评估是否为 `message_type` 筛选增加复合索引；当前百万行验收已证明主游标查询有界，但类型筛选仍需单独压测后决定，避免盲目增加写放大。
- [ ] 如果产品要求严格还原“发送当时实际投递范围”，设计不可变路由域或投递范围快照；当前第一版明确采用查询时的启用互联拓扑。

## 剩余客户端与兼容工作

- [ ] 决定同一浏览器 profile 的多标签页语义。当前标签页共享安装级 `client_instance_id`，新标签页会替换旧标签页；如需并存应增加标签页实例层级或使用 `sessionStorage`。
- [ ] iOS、Windows、macOS 按 Android 的实例 ID、Session、订阅和来源频道契约适配。
- [ ] PC 客户端确认多窗口/多进程语义，并区分安装实例与临时 Session。
- [ ] 客户端资源发布中声明所需最小服务端协议版本和能力位。
- [ ] 所有目标客户端升级后，逐步移除仅供 legacy 使用的 `Remove(username, ssid)`、冲突接口和旧群组消息查询路径。

## 剩余观测与运维

- [ ] 增加在线幽灵 Session 总数、平台/账号分布、legacy/modern 占比和每 Session 订阅数指标。
- [ ] 增加 fan-out 命中/去重、会话/订阅上限拒绝、无效 tag、端点不匹配、权限撤销和消息 API 查询指标。
- [ ] 日志只显示 `session_id` 的短形式，不记录完整 JWT、完整客户端实例标识或敏感网络信息。
- [ ] 管理后台提供受控的 Session 摘要查看和精确断开单个 Session 能力。
- [ ] 增加多端和多收灰度开关，并补消息查询频率、单页大小等部署级限额。
- [ ] 对热门频道补改造前后 UDP fan-out 延迟、队列丢弃率、缓存构建耗时、内存和锁竞争对比。

## 真机与发布验收

- [ ] 两台 Android 真机使用同一账号、同为 SSID `101`，可以同时在线且互不覆盖。
- [ ] Android A 发送到频道 1，Android B 订阅频道 1 时可收到；取消订阅后不再收到。
- [ ] Android 分别订阅频道 1、2、3，实时文本、语音和历史消息都进入真实来源频道。
- [ ] Web 与两台 Android 同账号同时在线，任一端下线、重连或切组不影响其他端。
- [ ] 一台 Android 切换 Wi-Fi/蜂窝网络、发生 NAT 重绑定或重新认证时，另一台保持在线。
- [ ] 多个频道同时出现真实语音时，验收 Web 混音和 Android 完整讲话流 FIFO 的可懂度、延迟、未听状态及回放行为。
- [ ] 在中心重启、边缘重连和滚动升级环境验证无会话覆盖、重复投递、串音或越权。
- [ ] 统计新版能力覆盖率，先灰度开启多端，再灰度开启多收，达到门槛后清理 legacy 路径。

当前结论：核心功能与自动化门禁已完成；生产完成定义仍取决于上述真机 E2E、观测能力和灰度发布验证。
