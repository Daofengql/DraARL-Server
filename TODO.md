# 幽灵设备多端在线、多频道接收与消息历史改造 TODO

> 本文替代旧 TODO，作为本次实时通信架构改造的实施清单。
>
> 核心目标：特殊幽灵设备允许多端在线，每个在线会话可以同时接收多个频道，但只能向一个选定频道发送；标准 UDP 实体设备继续保持单端在线。

## 需求原意

当前在线通信模型基本是“单听、单发”：一个在线设备只有一个当前群组，这个群组同时决定它向哪里发送以及能从哪里接收。通信记录接口又主要以当前用户及其设备为查询边界，因此 Web、Android 等客户端在通信列表中容易只看到自己设备产生的记录，不能稳定地把它当作完整的频道消息历史。

本次改造希望实现以下产品形态：

- 特殊幽灵设备取消单端限制，允许同一账号在多台移动设备、多个浏览器或其他客户端上同时在线。
- UDP 标准实体设备仍然只能单端在线。实体设备继续保持现有唯一身份和冲突保护，不能因幽灵设备支持多端而被一并放宽。
- 幽灵设备支持“多收单发”：可以同时订阅并接收多个频道/群组的消息，但发送时只能选择一个明确的目标频道。
- 同账号的多个幽灵客户端会话彼此独立；一台移动设备上线、切换频道或下线，不应覆盖或踢掉其他移动设备。
- 增加专用的频道消息历史 API。它与通信记录 API 可以来源于同一张 `comm_records` 表，但使用不同的查询条件、权限语义和响应模型。
- 通信记录继续服务于“本人设备发信记录、通信审计和管理员日志”；频道消息 API 则按群组查询该频道中有权查看的完整消息历史。
- Web、Android 及其他客户端查看消息时，应按群组读取其他发送者的历史消息，而不是只能读取当前账号设备的记录。
- 新的消息查询边界也应让消息日志、分页同步、语音回放、发送者展示和后续检索功能更容易维护。

这不是简单删除幽灵设备冲突判断。实现时必须把“账号/平台身份”“客户端安装实例”“临时在线会话”“唯一发送频道”和“多个接收订阅”拆开，同时保持实体设备单端规则不变。

## 0. 目标、边界与术语

### 0.1 必须保持的不变量

- [ ] 仅对幽灵设备开放多端在线：`DevModel/SSID 100-105`。
- [ ] UDP 幽灵设备 `101-104` 和 WebSocket 幽灵设备 `105` 均纳入新会话模型。
- [ ] 标准 UDP 实体设备继续使用 `owner_id + ssid` 作为唯一在线身份。
- [ ] 标准 UDP 实体设备的在线冲突检查、旧连接保护和节点迁移规则不得放宽。
- [ ] 一个幽灵设备会话只能拥有一个发送频道 `tx_group_id`。
- [ ] 一个幽灵设备会话可以订阅多个接收频道 `rx_group_ids`。
- [ ] `tx_group_id` 必须同时存在于 `rx_group_ids`，确保发送端可以监听所选频道。
- [ ] 发送权限与接收权限分别校验，不能因为可以查看群组就自动获得发言权。
- [ ] 私有群组必须持续满足群主、管理员或已验证成员条件。
- [ ] 禁用、删除、转为虚拟群组或撤销成员资格后，相关会话必须及时停止收发。
- [ ] 幽灵设备多端不得改变实体设备协议行为、数据库唯一约束和运行时索引语义。

### 0.2 术语

- [ ] `client_instance_id`：客户端安装实例的稳定随机 UUID；用于区分同一账号下的多台手机、多个桌面客户端或浏览器配置实例，不使用硬件指纹。
- [ ] `session_id`：每次在线连接由服务端签发的临时不可预测标识；断线重连可以生成新值。
- [ ] `session_tag`：UDP 数据包热路径使用的短会话标识；只用于快速定位已认证会话，不能替代 JWT 认证和所有权校验。
- [ ] `tx_group_id`：当前唯一发送频道。
- [ ] `rx_group_ids`：当前直接订阅的接收频道集合。
- [ ] `source_group_id`：消息实际发出时所在的源频道。
- [ ] `requested_group_id`：客户端正在查看的频道；互联转发时可能与 `source_group_id` 不同。

### 0.3 产品规则需要在编码前锁定

- [ ] 确认同一账号允许的幽灵设备最大在线会话数；建议默认 `8`，并支持配置。
- [ ] 确认单个会话最大接收频道数；建议默认 `16`，并支持配置。
- [ ] 确认“单发”的最终语义：最低要求是每会话只有一个 `tx_group_id`。
- [ ] 决定是否增加账号级语音话权锁；建议同一账号同一时刻只允许一个幽灵会话发起 PTT，避免两个移动设备同时发言。
- [ ] 明确文本消息是否受账号级话权锁限制；建议文本不占用语音 PTT 锁，但仍只能发往当前 `tx_group_id`。
- [ ] 明确订阅互联群组的语义；建议订阅实体群组后自动接收其当前互联通信域中的消息，并按会话去重。
- [ ] 明确多个无关频道同时出现语音时的客户端策略；建议不混音，采用“正在播放的流优先，其余流排队或提示未播放消息”。
- [ ] 明确消息历史使用“当前互联拓扑”还是“消息发送时的投递拓扑”；第一版可使用当前拓扑，但必须在 API 文档中说明。

## 1. 现有问题修复与安全基线

> 这一阶段必须先完成，不能等待多端功能上线。

### 1.1 通信记录越权修复

- [ ] 为 `GET /api/comm-records/:id` 增加记录级授权，禁止任意登录用户按递增 ID 获取其他用户录音。
- [ ] 管理员后台模式允许查看任意通信记录。
- [ ] 普通用户仅能通过“本人通信记录”语义访问本人设备或本人幽灵设备产生的记录。
- [ ] 群组消息详情不得复用上述本人记录授权，改由新的消息 API 按频道权限授权。
- [ ] 只有通过记录授权后才能生成或返回 MinIO/S3 音频签名 URL。
- [ ] 添加普通用户、群组成员、被移除成员、管理员和不存在记录的授权测试。

### 1.2 统一群组访问规则

- [ ] 删除 `comm_records.go` 中基于“拥有群组设备或曾经发过消息”的 `userHasGroupAccess` 授权语义。
- [ ] 消息历史、实时订阅、在线设备列表和切换发送频道统一复用 `canViewGroup`/`requireGroupViewAccess`。
- [ ] 群组必须存在、启用、非虚拟且类型受支持。
- [ ] 公开群组允许已审核用户访问。
- [ ] 私有群组只允许管理员、群主或已验证成员访问。
- [ ] 无权限时返回明确的 `403`，不得静默降级为“只返回本人记录”。
- [ ] 无效 `group_id` 返回 `400`，不存在的群组返回 `404`。
- [ ] 添加成员退出或被移除后无法继续读取历史、订阅实时消息的回归测试。

### 1.3 现有录音会话键修复

- [ ] 将录音缓冲的“运行时会话键”和最终持久化的 `device_id` 分离。
- [ ] 标准实体设备继续使用稳定的实体设备 ID 作为录音来源。
- [ ] Web 幽灵设备不得继续只使用 `-userID` 作为录音缓冲键。
- [ ] UDP 幽灵设备不得继续使用默认 `device_id=0` 作为所有幽灵设备共享的录音缓冲键。
- [ ] 幽灵设备录音缓冲使用唯一 `session_id` 或等价的内部会话键。
- [ ] 持久化到 `comm_records` 时，幽灵设备仍保存 `device_id=0`、实际 `user_id` 和平台 `device_ssid`。
- [ ] 添加两个幽灵设备在不同通信域同时发言时不会串音、合并或互相覆盖的测试。

## 2. 拆分频道消息 API

### 2.1 API 语义

- [ ] 保留 `/api/comm-records` 作为“本人发信记录/设备审计记录”接口。
- [ ] 保留管理员通过 `/api/comm-records?admin_mode=true` 查看全局通信记录的能力。
- [ ] 新增 `GET /api/groups/:group_id/messages`，专门获取频道消息历史。
- [ ] 新增 `GET /api/groups/:group_id/messages/:message_id`，专门获取单条频道消息及音频地址。
- [ ] 消息 API 不接受 `admin_mode`，管理员权限通过统一群组 ACL 判断。
- [ ] 消息 API 返回频道内所有可见发送者的消息，不按当前用户设备过滤。
- [ ] 消息 API 仅返回已完成记录 `status=2`；上传中、失败和不完整记录不进入普通消息流。
- [ ] 支持 `message_type=voice|text|all` 筛选。
- [ ] 支持稳定游标分页，不再以 offset/page 作为实时消息流的首选分页方式。
- [ ] 游标至少包含 `start_time + id`，保证相同时间戳下顺序稳定。
- [ ] 响应返回 `next_cursor`、`has_more` 和服务端时间。
- [ ] 为旧 Web/Android 迁移期保留现有 `/api/comm-records?group_id=...`，待客户端全部升级后再废弃群组消息语义。

### 2.2 消息响应模型

- [ ] 定义独立的 `MessageResponse`，不要直接暴露 `CommRecordResponse`。
- [ ] 返回 `id`、`message_type`、`source_group_id`、`requested_group_id`。
- [ ] 返回发送时间、结束时间、语音时长和消息状态。
- [ ] 返回结构化发送者信息：`user_id`、`username`、`callsign`、`nickname`、`ssid`、`dev_model`、`is_ghost`。
- [ ] 文本消息返回 `text_content`。
- [ ] 语音消息返回授权后的 `audio_url`、`audio_size` 和可选音频格式信息。
- [ ] 不要求客户端通过 `device_name` 字符串拆分呼号和 SSID。
- [ ] 明确所有时间字段的时区和格式；建议返回 RFC 3339 UTC，同时保留兼容字段到客户端迁移完成。

### 2.3 同表数据模型整理

- [ ] 第一阶段继续以 `comm_records` 作为语音与文本消息的共同来源，不新建重复消息主表。
- [ ] 增加明确的 `message_type` 字段，停止长期依赖 `audio_path` 的 `text:` 前缀判断。
- [ ] 增加独立 `text_content` 字段，允许正常的 UTF-8 文本长度，不再受 `varchar(255)` 音频路径限制。
- [ ] 保留旧 `text:` 数据的兼容读取和一次性迁移脚本。
- [ ] 评估并增加发送者快照字段，至少包含发送时的呼号、用户名、昵称和设备型号。
- [ ] 设备删除、转让或用户修改呼号后，历史消息显示不得被错误改名或误判成幽灵设备。
- [ ] 保留 `group_id` 作为 `source_group_id` 的现有存储语义，并在代码层统一命名。
- [ ] 评估索引 `(group_id, status, start_time, id)`，使用真实数据量执行 `EXPLAIN` 后再落迁移。
- [ ] 为文本类型筛选增加合适索引，避免使用 `audio_path LIKE 'text:%'` 扫描大表。

### 2.4 互联群组历史

- [ ] 第一版复用当前启用的互联关系计算频道可见消息集合。
- [ ] 请求频道本身必须先通过 ACL；不能因为查询互联关系绕过私有群组授权。
- [ ] 明确私有群组与公开群组互联后，实时投递和历史可见范围必须一致。
- [ ] 同一条消息通过多个互联路径可达时只返回一次。
- [ ] 响应中保留真实 `source_group_id`，客户端可显示消息来自哪个频道。
- [ ] 如果需要严格还原“发送当时实际投递范围”，设计不可变的路由域/投递范围快照；不能用当前互联拓扑伪造历史事实。

### 2.5 查询实现与测试

- [ ] 将通信记录查询和频道消息查询放入独立 repository/service，避免在 Gin handler 中继续堆叠 SQL 与权限分支。
- [ ] 统一 JOIN 发送者、设备和群组信息，处理已删除关联记录。
- [ ] 添加公开群组、私有群组、互联群组、禁用群组和已删除发送设备测试。
- [ ] 添加游标边界、同时间戳、多页并发插入和去重测试。
- [ ] 添加消息详情不能越权获取音频 URL 的端到端测试。

## 3. 幽灵客户端实例与在线会话模型

### 3.1 稳定客户端实例

- [ ] Web、Android、iOS、Windows 和 macOS 客户端生成随机 UUID `client_instance_id`。
- [ ] `client_instance_id` 在单次安装范围内持久化；清除应用数据后允许重新生成。
- [ ] 禁止使用 IMEI、Android ID、MAC 地址或其他硬件指纹替代随机 ID。
- [ ] JWT 认证时提交 `client_instance_id` 和客户端能力版本。
- [ ] 老客户端没有 `client_instance_id` 时进入 legacy 单实例槽位，并继续保持同平台单端限制。
- [ ] 新客户端不得仅凭 `client_instance_id` 获得权限；始终先验证 JWT 和账号状态。

### 3.2 临时会话

- [ ] 新增统一 `GhostSession` 领域模型。
- [ ] 字段至少包含 `session_id`、`session_tag`、`client_instance_id`、`owner_id`、`username`、`callsign`、`dev_model`、`ssid`。
- [ ] 字段至少包含 `transport`、连接对象/UDP 地址、创建时间、最后活动时间和连接状态。
- [ ] 字段至少包含 `tx_group_id`、`rx_group_ids`、`disable_send`、`disable_recv` 和能力集合。
- [ ] `session_id` 使用服务端生成的不可预测标识，不使用 `userID`、SSID 或可枚举自增 ID。
- [ ] `session_tag` 只在当前在线会话集合内唯一，并与账号、平台、地址和会话状态联合验证。
- [ ] 一个 `client_instance_id` 可以重连并替换自己的旧会话，但不能替换同账号的其他客户端实例。
- [ ] 旧会话延迟清理时只能删除自己，不能误删重连后的新会话。
- [ ] 达到会话上限时返回明确错误和当前会话摘要，不随机踢掉其他移动设备。

### 3.3 偏好持久化

- [ ] 现有 `user_device_preferences(user_id, dev_model)` 继续作为 legacy 客户端的默认发送频道。
- [ ] 为多实例客户端设计按 `user_id + dev_model + client_instance_id` 保存的偏好模型。
- [ ] 持久化每个客户端实例的最后 `tx_group_id` 和 `rx_group_ids`。
- [ ] 决定使用主表加订阅关系表，避免把不可查询的逗号字符串写入数据库。
- [ ] 客户端首次升级时，以现有平台 `last_group_id` 初始化 `tx_group_id` 和首个接收频道。
- [ ] 删除用户时级联删除实例偏好和订阅。
- [ ] 删除群组时清理相关发送偏好与接收订阅。
- [ ] 服务启动时不得把持久化偏好误认为在线会话。

## 4. UDP 幽灵设备多端协议

### 4.1 协议扩展

- [ ] 为幽灵 JWT 认证定义版本化 payload，携带 `client_instance_id`、能力位和协议小版本。
- [ ] 保持旧版“DATA 仅为 JWT 字符串”的认证兼容路径。
- [ ] 认证成功响应返回 `session_id`、UDP `session_tag`、初始 `tx_group_id` 和 `rx_group_ids`。
- [ ] 旧客户端仍能读取认证响应首字节状态码；新增字段不得使旧客户端把成功响应误判为错误详情。
- [ ] 明确 DraARLv1 头部 `Reserved[4]` 的新语义并更新协议文档。
- [ ] 建议客户端上行包使用 `Reserved[4]` 携带 `session_tag`。
- [ ] 建议服务端下行语音/文本包使用 `Reserved[4]` 携带 `source_group_id`。
- [ ] 认证和心跳响应中的 Reserved 语义必须单独定义，不能与媒体下行混淆。
- [ ] 对超出 32 位表示范围的群组 ID 明确拒绝；数据库和 API 同步约束群组 ID 范围。
- [ ] 对 `session_tag` 猜测、重放、地址变更和伪造包进行安全评审。
- [ ] UDP 地址发生 NAT 重绑定时要求重新认证或执行带凭证的安全重绑定，不能只凭 username/SSID 抢占会话。

### 4.2 UDP 运行时索引

- [ ] `UDPGhostManager.devices` 从 `username-ssid` 单值索引改为按 `session_id` 管理。
- [ ] 增加 `session_tag -> session` 热路径索引。
- [ ] 增加 `owner_id + dev_model + client_instance_id -> current session` 重连索引。
- [ ] 增加 `group_id -> session_id -> session` 接收订阅索引。
- [ ] 同一会话订阅多个群组时允许出现在多个接收索引中。
- [ ] 更新订阅时以一次锁操作完成旧索引删除和新索引添加，不能出现长期半更新状态。
- [ ] UDP 超时、主动下线、JWT 失效和实例重连均按 `session_id` 精确清理。
- [ ] `Remove(username, ssid)` 等旧 API 仅服务 legacy 路径，逐步替换为 Session API。
- [ ] 保留标准实体设备的 `owner_id + ssid` 运行时索引和冲突逻辑，不迁移到 GhostSession 多实例语义。

### 4.3 UDP 包鉴权与路由

- [ ] 每个幽灵设备上行心跳、语音和文本包必须定位到唯一已认证 Session。
- [ ] 校验包中 username、SSID、DevModel、session tag 和网络端点与 Session 一致。
- [ ] 上行消息的目标频道只能取服务端 Session 的 `tx_group_id`，不得相信客户端在媒体包中自行声明任意群组。
- [ ] 下行 fan-out 根据 `rx_group_ids` 选择 Session，而不是比较单一 `Device.GroupID`。
- [ ] 发送源排除改为精确排除当前 `session_id`；同账号同平台的其他移动设备应正常收到消息。
- [ ] 一个 Session 因多个订阅或互联路径命中时只发送一个 UDP 包。
- [ ] 接收禁用、会话过期或群组权限失效时不得进入 fan-out 快照。
- [ ] 接收索引变化后正确失效 `domainReceiverCache`。
- [ ] fan-out 缓存项携带 Session 身份，不能再以 `username + ssid` 认定所有同平台客户端都是发送源。

## 5. WebSocket 幽灵设备多端

### 5.1 连接管理器

- [ ] 删除 WebSocket 握手阶段“同一用户已有幽灵设备在线即 409”的强制冲突规则。
- [ ] `WSConnectionManager.ghostDevices` 从 `userID -> device` 改为 `sessionID -> device`。
- [ ] 增加 `userID -> set(sessionID)` 辅助索引，用于状态查询、限额和账号下线。
- [ ] `getDeviceKey` 和 `GetIdentifier` 纳入 `session_id`，不再只返回 `ghost-userID`。
- [ ] 全局群组索引按接收订阅维护，而不是按单一 `GroupID` 维护。
- [ ] 广播到多个群组时按 `session_id` 去重。
- [ ] 断开连接、心跳超时和写队列失败时只清理对应 Session。
- [ ] 评估合并 `GhostDeviceManager` 与 `WSConnectionManager` 中重复的单用户设备状态，避免两套索引再次不一致。

### 5.2 WebSocket 协议

- [ ] 认证成功消息返回 `session_id`、`client_instance_id`、`tx_group_id` 和 `rx_group_ids`。
- [ ] 增加更新订阅成功、订阅被撤销和发送频道变化事件。
- [ ] 下行语音/文本消息携带 `source_group_id`。
- [ ] 保持旧二进制 DraARLv1 消息兼容，定义支持多频道元数据的新能力位。
- [ ] 旧 Web 客户端没有多频道能力时只订阅一个频道。
- [ ] 多标签页使用不同临时 `session_id`；是否共享持久化 `client_instance_id` 和默认订阅由产品规则决定。

## 6. 发送频道与接收订阅 API

### 6.1 会话 API

- [ ] 新增 `GET /api/radio/sessions`，仅返回当前账号自己的幽灵在线会话。
- [ ] 返回平台、客户端实例、连接类型、在线时间、最后活动时间、发送频道和接收频道。
- [ ] 新增 `PUT /api/radio/sessions/:session_id/routing`。
- [ ] 请求体使用 `tx_group_id` 和去重后的 `rx_group_ids`。
- [ ] 更新前一次性验证所有群组存在、启用、非虚拟并且当前用户有权访问。
- [ ] 整个更新在逻辑上原子化；任一群组失败时不修改运行时或持久化状态。
- [ ] 先提交持久化偏好，再投影到运行时索引；持久化失败不得留下临时路由状态。
- [ ] 更新成功后返回服务端规范化后的完整路由状态。
- [ ] 新增 `DELETE /api/radio/sessions/:session_id`，允许用户主动断开自己的指定会话。
- [ ] 可选增加“断开其他会话”操作，但不能默认在新连接时踢掉所有旧端。

### 6.2 旧 API 兼容

- [ ] `/api/radio/group` 对 legacy 客户端继续按 `dev_model` 更新单一群组。
- [ ] 新客户端调用旧接口时可携带 `session_id`；存在多个同平台 Session 且未指定 Session 时返回 `409 ambiguous_session`。
- [ ] `/api/radio/status` 保留旧单会话响应到客户端迁移完成。
- [ ] 新增或扩展状态接口返回会话列表，不能随意选择一个连接伪装成唯一在线端。
- [ ] `/api/radio/conflict` 对新多端客户端不再报告同平台冲突；legacy 客户端维持原行为。
- [ ] 更新错误码文档：会话上限、会话不存在、会话归属错误、订阅上限、频道权限失效和会话歧义。

## 7. 半双工与单发约束

- [ ] 每个 Session 的语音和文本上行始终路由到唯一 `tx_group_id`。
- [ ] 保留现有通信域半双工仲裁，防止同一群组/互联域出现多个讲话者。
- [ ] 半双工讲话者 key 纳入 Ghost Session，不能把同用户同平台的多个会话误认为同一个正在讲话的源。
- [ ] 如果启用账号级 PTT 锁，以 `owner_id` 为 key 建立带租约和超时的原子话权状态。
- [ ] 账号级 PTT 锁与通信域话权锁的获取顺序固定，避免死锁和部分占用。
- [ ] 任一话权获取失败时释放已获得的另一层租约。
- [ ] 断线、超时、切换发送频道和 PTT 结束时及时释放话权。
- [ ] 同一 Session 在 PTT 期间禁止切换 `tx_group_id`；返回明确冲突状态。
- [ ] 添加同账号双端、不同账号同域、同账号不同域和互联域并发测试。

## 8. 节点互联与边缘接入

> 多端会话必须覆盖中心直连和边缘节点，不能只在单机模式可用。

- [ ] 现有实体设备互联身份继续保持 `owner_id + ssid`。
- [ ] 幽灵设备互联身份扩展为 `owner_id + ssid + client/session identity`。
- [ ] `DeviceGrant`、中心 active identity、Session epoch 和撤销消息增加可选幽灵实例字段。
- [ ] legacy 实体设备与 legacy 幽灵设备保持旧身份编码，避免无条件破坏节点协议。
- [ ] 中心不得因为第二个同平台幽灵 Session 上线而迁移或撤销第一个 Session。
- [ ] Session 路由携带唯一发送频道和接收订阅所需信息。
- [ ] Relay 上行继续以服务端 Session 的 `tx_group_id/domain_id` 为准。
- [ ] Relay 下行向边缘多订阅 Session 投递时按 Session 去重。
- [ ] 节点会话报告、批量确认、重连恢复和撤销按扩展身份精确处理。
- [ ] `routesync.PublishIdentity` 等仅按 `user_id + dev_model` 发布的路径改为支持幽灵实例，不影响实体设备。
- [ ] 设计节点协议能力协商；旧边缘节点不支持多端时应明确降级或拒绝，不得静默覆盖会话。
- [ ] 制定滚动升级顺序：中心先兼容新旧协议，再升级边缘，最后开启多端开关。
- [ ] 增加中心直连、边缘直连、边缘切换、控制链路断线恢复和跨节点同账号多端测试。

## 9. Web 客户端改造

- [ ] 生成并持久化 Web `client_instance_id`。
- [ ] 认证后保存当前标签页的 `session_id`。
- [ ] 移除“账号已在其他页面建立电台连接”的单端阻断 UI。
- [ ] 增加发送频道选择与接收频道多选控件。
- [ ] 明确显示当前唯一发送频道。
- [ ] 接收频道达到上限或权限失效时给出可操作错误状态。
- [ ] 实时消息按服务端下行的 `source_group_id` 入库和分栏，不使用当前选中频道猜测来源。
- [ ] 同一 Session 通过互联或多个订阅重复收到同一消息时按消息/帧标识去重。
- [ ] 历史消息改用 `/api/groups/:group_id/messages`。
- [ ] 移除前端对 `device_name` 的 SSID 字符串拆分，使用结构化 sender。
- [ ] 使用游标加载更早消息，并处理实时消息与历史消息合并。
- [ ] 支持查看其他接收频道的未读消息，不自动把所有频道音频同时播放。
- [ ] 多标签页分别断开时不得影响同账号其他标签页或移动端。

## 10. Android 客户端改造

- [ ] 生成并安全持久化 Android `client_instance_id`。
- [ ] 扩展 UDP JWT 认证请求与成功响应解析。
- [ ] 在后续上行心跳、文本和语音包中携带 `session_tag`。
- [ ] 解析下行媒体包的 `source_group_id`，不得继续使用当前 `selectedGroupId` 代替来源频道。
- [ ] 本地消息缓存继续按 `account_key + group_id` 隔离，但以真实来源频道保存消息。
- [ ] 增加唯一发送频道和多接收频道设置。
- [ ] 使用 Session routing API 原子更新 `tx_group_id/rx_group_ids`。
- [ ] App 退到后台后继续维持自己的 Session，不受其他手机上线影响。
- [ ] 移除 `JWTAuthGhostDeviceConflict` 作为新版 Android 同平台正常多端场景的等待逻辑。
- [ ] legacy 服务端仍返回冲突时保留兼容提示。
- [ ] 历史同步改用新的群组消息 API 和游标。
- [ ] 使用结构化 sender 字段判断本人消息，不再依赖 `device_name` 和固定字符串解析。
- [ ] 接收语音组装从单个全局 incoming stream 改为按 `source_group_id + sender identity` 隔离。
- [ ] 定义并实现多频道同时出现语音时的播放、排队、未读和自动播放策略。
- [ ] 两台 Android 手机使用同一账号时均能在线、独立切换发送频道和订阅集合。
- [ ] 一台手机断线、切换网络或重认证不得导致另一台手机离线。

## 11. 其他原生客户端

- [ ] iOS、Windows、macOS 按与 Android 相同的实例 ID、Session、订阅和来源频道契约实现。
- [ ] PC 客户端确认是否存在多窗口/多进程场景，并使用安装实例 ID + 临时 Session 区分。
- [ ] 未升级客户端始终走 legacy 单实例槽位，不得与新版索引互相覆盖。
- [ ] 客户端资源分发中增加所需最小服务端协议版本和能力声明。

## 12. 观测、限流与运维

- [ ] 增加在线幽灵 Session 总数、按平台数量、按账号数量和 legacy/new 占比指标。
- [ ] 增加每 Session 接收订阅数和 fan-out 命中/去重数量指标。
- [ ] 增加会话上限拒绝、订阅上限拒绝、无效 session tag、地址不匹配和权限撤销计数。
- [ ] 增加消息 API 查询耗时、扫描行数、游标错误和音频授权失败指标。
- [ ] 日志使用 `session_id` 的短显示形式，不记录完整 JWT 或敏感会话凭证。
- [ ] 管理后台可查看账号的幽灵在线会话，但默认隐藏完整客户端实例标识和网络敏感信息。
- [ ] 提供管理员精确断开单个幽灵 Session 的能力。
- [ ] 增加配置开关，允许部署初期关闭幽灵多端或多频道订阅。
- [ ] 配置会话数、订阅数、消息查询频率和单页大小上限。
- [ ] 评估多频道订阅对 UDP fan-out 快照、WS 写队列、内存和带宽的放大影响。

## 13. 测试与验收矩阵

### 13.1 服务端单元测试

- [ ] 同账号、同平台的两个新版幽灵 Session 可同时注册。
- [ ] 同账号、不同平台的多个 Session 继续正常注册。
- [ ] legacy 同账号同平台第二个幽灵连接仍按旧规则冲突。
- [ ] 标准实体 UDP 设备第二个网络端点仍被拒绝。
- [ ] 一个幽灵 Session 可以进入多个接收索引。
- [ ] 更新订阅后旧索引完整清理，新索引完整建立。
- [ ] 多订阅和互联路径不会造成重复投递。
- [ ] 发送源只排除当前 Session，同账号其他端仍能收到。
- [ ] 禁收、禁发、群组禁用和成员撤销立即生效。
- [ ] 录音缓冲在多 Session、跨平台和跨通信域下不串流。
- [ ] `go test -race` 覆盖注册、断开、更新订阅、fan-out 和超时清理并发路径。

### 13.2 API 集成测试

- [ ] 消息列表返回频道内其他用户的语音和文本。
- [ ] 通信记录默认仍只返回本人发信记录。
- [ ] 消息详情和音频 URL 不能跨群组越权。
- [ ] 私有群组成员退出后列表、详情和订阅全部返回 `403`。
- [ ] 游标翻页期间插入新消息不产生遗漏或重复。
- [ ] 多 Session routing 更新只影响指定 Session。
- [ ] 多 Session 未指定 `session_id` 调旧切组接口返回明确歧义错误。
- [ ] 删除一个 Session 不影响同账号其他 Session。

### 13.3 端到端场景

- [ ] 两台 Android 手机同账号、同为 SSID 101，同时在线。
- [ ] Android A 发送到群组 1，Android B 同时订阅群组 1 时可以收到。
- [ ] Android B 未订阅群组 1 时不会收到。
- [ ] Android A 订阅群组 1、2、3，收到消息后分别进入正确频道历史。
- [ ] Web 与两台 Android 同账号同时在线，任一端下线不影响其他端。
- [ ] 两个互联群组同时被订阅时，同一帧只投递一次。
- [ ] 两个无关群组同时讲话时，服务端不串流，客户端按既定策略播放。
- [ ] 实体 UDP 设备尝试第二端上线时仍保持旧设备在线并拒绝新设备。
- [ ] 中心与边缘各存在一个同平台幽灵 Session 时不会互相迁移或撤销。
- [ ] 网络切换、NAT 端口变化、JWT 刷新和服务重启后的行为符合会话恢复设计。

### 13.4 性能验收

- [ ] 构造至少 1,000 个幽灵 Session、每 Session 多频道订阅的 fan-out 压力测试。
- [ ] 比较改造前后的 UDP fan-out 延迟、队列丢弃率、缓存构建耗时和内存占用。
- [ ] 验证单个热门频道不会因多订阅索引产生全局锁竞争。
- [ ] 验证 WS 慢客户端不会阻塞其他 Session，并保持写队列有界。
- [ ] 对百万级 `comm_records` 执行消息列表与详情查询计划检查。
- [ ] 验证消息 API 的游标查询不随页数增加出现线性退化。

## 14. 文档、迁移与发布

- [ ] 更新 `docs/Protocol.md` 中幽灵设备认证、Reserved 字段、Session 和来源频道定义。
- [ ] 更新 `docs/api/05-无线电与实时通信.md` 中会话与 routing API。
- [ ] 更新 `docs/api/06-通联记录与日志.md`，区分通信记录和频道消息历史。
- [ ] 更新 `docs/api/09-完整路由索引.md` 与错误码文档。
- [ ] 更新 `docs/api/10-WebSocket协议详解.md`。
- [ ] 更新节点互联协议及能力协商说明。
- [ ] 编写数据库迁移、旧文本记录迁移和回滚说明。
- [ ] 发布顺序采用“数据库兼容迁移 -> 服务端兼容代码 -> 中心/边缘节点 -> Web -> Android/原生客户端 -> 开启功能开关”。
- [ ] 在旧客户端占比较高时保持 legacy 单实例路径和旧 API。
- [ ] 统计新版能力覆盖率，达到发布门槛后再逐步废弃 `/api/radio/conflict` 和 comm-records 群组消息查询。
- [ ] 回滚时只关闭多端/多订阅入口，不删除新会话偏好和消息字段，确保可再次启用。

## 15. 推荐实施顺序

- [ ] P0：修复通信记录详情越权、统一群组 ACL、修复幽灵录音缓冲键。
- [ ] P1：新增频道消息 API、显式消息字段和游标分页。
- [ ] P2：Web 与 Android 切换到频道消息 API，停止把通信记录接口当聊天历史接口。
- [ ] P3：实现统一 GhostSession、实例偏好和会话 routing API。
- [ ] P4：实现 WebSocket 多端与多频道订阅。
- [ ] P5：扩展 UDP 协议、UDP 幽灵多端、来源频道元数据和 Android 多频道接收。
- [ ] P6：扩展节点互联身份与边缘多 Session 路由。
- [ ] P7：完成全量端到端、竞态、压力和滚动升级测试。
- [ ] P8：灰度开启多端，再灰度开启多频道订阅，最后清理 legacy 兼容代码。

## 16. 完成定义

- [ ] 同一账号的多台 Android 设备可以同时在线且互不覆盖。
- [ ] 同一账号的 Web、Android 和其他幽灵客户端可以同时在线且互不覆盖。
- [ ] 每个幽灵 Session 可以独立配置一个发送频道和多个接收频道。
- [ ] 所有实时消息都携带可识别的真实来源频道。
- [ ] Web 与 Android 按频道显示、缓存和同步其他用户的消息历史。
- [ ] 通信记录与频道消息 API 的权限、返回模型和产品用途清晰分离。
- [ ] 标准 UDP 实体设备仍严格保持单端在线。
- [ ] 中心、边缘和互联场景下均不存在会话覆盖、重复投递、串音或越权。
- [ ] 旧客户端在兼容期可以继续使用原有单端单频道能力。
- [ ] 协议、API、数据库迁移、监控、测试和回滚文档全部完成。
